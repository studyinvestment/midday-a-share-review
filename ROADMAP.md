# 午间复盘 Skill · 下一阶段路线图（v2 信任升级）

> 状态：0→1 已完成（能采集、能生成、结构稳定、移动端可读）。
> 本阶段目标不是"加指标"，而是三件事：**时间一致性 / 数据可审计 / 叙事有条件**。
> 原则：稳字当头，逐级验证，不跳跃式大改。

## 实施进度
- ✅ **P1 时间一致性**：`--mode` 三态（strict-midday / late-snapshot / render-archive）+ 自动判定；资讯 09:00–11:30 过滤 + 重复标题聚类；warn 框与所有模块"上午"标签随模式联动。
- ✅ **P1 数据可审计**：schema_version=2 + meta(mode/collected_at) + quality + sources；`--date`/窗口/指数齐全/分钟末点/广度等式/资讯时段 校验，核心缺失→非零退出；可选缺失→"数据不可用"。
- ✅ **P1 广度分母 + TLS**：listed/valid/missing 拆分，上涨占比按有效样本；恢复证书验证。
- ✅ **P2 事实层 + 条件叙事**：`compute_facts()` 事实层（breadth/volume/index/leader/news/zt/weekday/资金流排名）；所有硬编码因果句清除（资源与成长共振/周五效应/主力派发/下方空间有限/确定性最高/温和缩量是核心原因 等）；开盘方式、成本距离、资金流强度均条件化。
- ⬜ **P3 持续性校准**：`persist()` 已改名为"盘中强度/共振评分"（不承诺预测性，✅已做）；二期回测校准（待积累数据）。
- ✅ **三态夹具**：`tests/` 下构造普涨(82%)/分化(46%)/普跌(18%) 3 份 v2 夹具（已剔除隐私持仓代码、纠正字符串→数值类型、丢弃过期键），`run_three_state.py` 断言三档 breadth_regime / zt_regime 叙事分支正确、上涨占比与注入一致、非周五 weekday 分支正常；全绿 ✅。
- ✅ **Codex 清理**：持仓去固化、deploy 移 `tools/`、SKILL.md `agent_created` 入 `metadata`、`present_files` 改为环境能力、数据来源记真实提供方、盈利徽章动态、持仓点评条件化、资金流改排名。

## 一、三大优先级

### P1 时间一致性（Time consistency）
**核心问题**：午后补跑会混合不同时点。
- 实测 15:54 补跑：指数分时正确还原到 11:30；指数"现价"列已警告（正确）；
  但板块/涨跌家数/资金流/涨跌停池/资讯仍标"上午/午间"，而筛出的 13 条"上午资讯"里 12 条发生在 11:30 之后。
- 根因：`generate_midday_review.py` 的 warn 框（line 799–802）**只覆盖"现价"列**，
  其余模块的"上午"标签未随运行时刻联动（line 807/818/824/883/947 等硬编码"上午"）。

**运行模式明确三分**：
- `strict-midday`：**默认**。仅允许当天约 11:30–13:00 采集；其余时段拒绝并提示。
- `late-snapshot`：13:00 后运行。板块/广度/资金流/涨跌停/资讯**明确标成"当前快照"**，禁止叫"上午"。
- `render-archive`：用中午已保存的 JSON 重新渲染。这是**唯一可信的历史补跑**方式。

### P1 数据可审计（Data auditability / 质量闸门）
现状：接口失败常变成空列表或 0 值，但程序仍打印 `[OK]` 并生成"看起来完整"的报告。
新增采集结果元数据结构（schema_version=2）：
```json
{
  "schema_version": 2,
  "meta": {"report_date":"2026-08-21","collected_at":"2026-08-21 11:35:00+08:00","mode":"strict-midday"},
  "quality": {"level":"pass","errors":[],"warnings":[]},
  "sources": {"minutes":{"status":"ok","as_of":"11:30","coverage":"7/7"},
              "breadth":{"status":"ok","as_of":"11:35"},
              "news":{"status":"ok","as_of":"11:30"}}
}
```
**最低验证（缺失即非零退出码，不生成伪完整报告）**：
- `--date` 必须等于采集数据日期（禁止今天分时写入昨天文件）。
- 7 个指数齐全 + 分钟末点为 11:30。
- `up + down + flat + missing == listed_total`。
- 资讯属于报告日期且不晚于 11:30。
- 核心数据缺失 → 返回非零退出码，**不生成报告**。
- 可选数据缺失 → 显示"数据不可用"，**不能显示为 0**。

**广度分母 bug**（实测）：`5901 != 2505 + 2862 + 176`。
应拆为：`listed_total=5901`、`valid_total=5543`、`missing=358`；**上涨占比以有效样本计算**。

**TLS 修复**：`collect_midday_data.py` line 22 全局关闭证书验证（`ssl._create_unverified_context`）。
金融数据不建议如此；恢复证书验证，仅保留 IPv4 强制 + 备用域名降级。

### P2 叙事有条件（Conditional narrative / 事实与文案分离）
现状：大量结论硬编码，未必由数据支持：
- "资源与成长主线共振"、"资源与光模块主线可继续走强"、"市场宽度极差"、
  "温和缩量（是冲高回落核心原因）"、"多数连板股盘中开板"、"周五效应"、
  "上午低点支撑有效、下方空间有限"、"典型主力派发、散户接盘"。
- 其中"周五效应"甚至会在周一照样出现。

**最简改法（不引入大模型）**：先生成结构化事实层 `facts`，再由条件模板生成文字。
```python
facts = {
  "breadth_regime": "weak",        # weak/neutral/strong
  "volume_regime": "expanded",     # expanded/neutral/contracted
  "index_pattern": "dip_then_rebound",
  "leader_concentration": "high",
  "news_confidence": "medium"
}
```
每句判断须可追溯到：用了哪些指标 / 阈值是什么 / 数据缺失时是否停止 / 属于事实·相关性·推测。
**措辞纪律**：少用"核心原因/主力派发/散户接盘/下方空间有限"，改用"同时出现/可能反映/需进一步验证"。

### P3 校准"持续性"核心卖点
`persist()`（line 143–167）本质是**盘中强度评分**，却命名"持续性"。
单日净流入/涨跌比/龙头涨幅未必预测下午或次日。
- 路线1（推荐先做）：改名为"盘中强度/共振评分"，**不承诺预测性**。
- 路线2（二期）：保存每天午盘快照，回测评分 vs 当日下午/次日收益，再定阈值与置信度。
- 先走路线1，积累几十个交易日后再做路线2。

## 二、最小版本实施顺序（用户建议）
1. 修复时间模式 + 资讯过滤（09:00–11:30，重复标题聚类）。
2. 增加数据质量验证 + 来源时间戳（schema_version=2 + quality + sources）。
3. 清除无条件硬编码叙事 → 事实层 + 条件模板。
4. 修复广度分母（listed/valid/missing）+ 恢复 TLS 证书验证。
5. 用三份固定数据夹具测试"普涨 / 分化 / 普跌"三种市场状态。

完成后：从"漂亮的日报生成器"升级为"真正可信的午盘决策流水线"。

## 三、Codex 兼容性清理（随 roadmap 一并处理）
- `deploy_to_github.py` 属仓库维护工具，**不应随运行时 skill 安装** → 移出 `scripts/` 到 `tools/` 或独立维护仓库。
- SKILL.md line 4 `agent_created: true` 顶层字段通不过 Codex 标准校验 → 移入 `metadata` 块，或为 WorkBuddy / Codex 分别提供清单。
- SKILL.md line 3/69-70/99 把 `present_files` 写成跨平台"必须调用" → 改为"使用当前环境可用的文件交付能力"。
- PAT 经命令行参数传入会留在 shell 历史 → 改用环境变量 / 交互式输入，或文档提示风险。
- 数据来源区（line 948）固定声称 MCP 失败，但脚本并未检测 MCP → 改为记录真实使用的数据提供方（公开 API 降级）。
- 盈利持仓徽章（line 577）固定写"浮亏" → 动态显示浮盈/浮亏。
- 持仓点评（line 464 "平开后"、line 481 "距成本仍有较大距离"）→ 条件判断。
- 资金流"主力净流入 10 亿"类绝对阈值对大小板块不公平 → 改为排名/分位数/相对成交额。
