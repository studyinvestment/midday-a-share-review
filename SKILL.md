---
name: midday-a-share-review
description: 生成 A股午间复盘自包含 HTML 报告。固化 8 大模块结构（指数行情/走势叙事/板块TOP5/量能/资讯/涨停/持仓诊断/下午推演）、暗色渲染模板与中国配色。数据源四级降级链：westock-mcp / tdx-connector → hithink-finance MCP（同花顺）→ 腾讯财经 + 东方财富公开 API；脚本层不依赖任何 MCP，无连接器环境走公开 API 亦可用。持仓由外部 holdings.json 提供，不随模板分发。遵循 Agent Skills 开放标准，WorkBuddy / Codex / 其他通用 Agent 同一份文件可直接加载。适用于午间 12:00 定时复盘自动化，输出可直接推送手机端。
compatibility: "Python 3.9+（仅标准库，无第三方依赖）；需可访问 qt.gtimg.cn / web.ifzq.gtimg.cn / push2.eastmoney.com。MCP 连接器（westock-mcp / tdx-connector / hithink-finance）为可选增强，非必需。"
metadata:
  agent_created: true
  version: 4.10
  trust: "时间一致性 / 数据可审计 / 叙事有条件"
---

# 午间 A股 复盘（结构固化版）

把"午间复盘"做成**可复用的标准件**：无论哪一天运行，都产出结构一致、渲染一致、逻辑一致的 8 模块 HTML 报告。
本 skill 包含两个脚本：`collect_midday_data.py`（采集）与 `generate_midday_review.py`（渲染），均为 **date-agnostic**（读 `--date`，默认今天）。

## 何时使用
- 用户要求"午间复盘 / 午盘复盘 / 中午盘面总结"，或触发"午间早盘复盘"自动化。
- 需要一份**自包含、无外部依赖**的 HTML（内联 SVG 分时图/条形图，CSS 暗色主题），便于推送与归档。
- 当 westock-mcp / tdx-connector 不可用时，本 skill 已内置**四级降级链**（见下节），无需另行处理。

## 跨环境安装与运行（Agent Skills 开放标准）

本 skill 遵循 **Agent Skills 开放标准**（目录内 `SKILL.md` + `name`/`description` frontmatter），
`SKILL.md` 与 `scripts/` 是**同一份文件**，在 WorkBuddy、Codex、Cursor、Gemini CLI、Copilot 等环境
**无需改动**即可加载。安装 = 把整个目录放到对应 skills 路径：

| 环境 | 作用域 | 路径 |
| --- | --- | --- |
| WorkBuddy | 用户级 | `~/.workbuddy/skills/midday-a-share-review/` |
| Codex CLI | 用户级 | `~/.agents/skills/midday-a-share-review/` |
| Codex / Cursor / Gemini CLI / Copilot 等 | 项目级 | `<repo>/.agents/skills/midday-a-share-review/` |

调用：`$midday-a-share-review`（显式）或"午间复盘"（由 description 隐式匹配）。
Codex 侧展示元数据见 `agents/openai.yaml`；完整安装说明与路径注意事项见 `README.md`。

**⚠️ 先判环境，再选数据源（影响数据精度）：**
- 降级链**第 1–3 级是 WorkBuddy 连接器专属**（`westock-mcp` / `tdx-connector` / `hithink-finance`）。
- **Codex 等无连接器环境：只有第 4 级（公开 API）可用** —— 这正是两个脚本内置的默认路径，
  **开箱即用，不需要任何 MCP 或 API key**。代价：板块资金流 5/20 日维度、部分指数快照字段缺失。
- 脚本层**不依赖任何 MCP**，两种环境都产出结构一致的报告；差异只体现在数据精度与文末 `sources` 区。
  **缺什么就在来源区如实写什么，不得因字段缺失而虚报"数据完整"。**

## 8 大模块结构（已固化，顺序不可随意变动）
1. **上午主要指数行情** — 7 大指数表（上午收盘/涨跌幅/开盘/最高/最低/振幅/现价）+ 结构特征点评 + 各指数分时卡。
2. **上午走势叙事与阶段驱动** — 按时间轴分 4–5 阶段（开盘→早盘→盘中→尾盘→主线），每阶段标注驱动因素。数据驱动自动生成。
3. **领涨/领跌板块 TOP5 与盘中强度评分** — 板块卡含主力净额/涨跌比/龙头 + 强度标签（高/中/弱，基于 `persist()` 盘中强度打分，**不承诺预测性**）+ 行业涨幅/跌幅 TOP10 条形图 + 概念 TOP8 表。
4. **成交额与量能评估** — 两市成交额、沪/深上午成交量 vs 前5日全日均量、推算全日量能；量价背离警示。
5. **上午重要资讯与市场影响** — 按关键词筛选财经要闻，标注影响板块与方向（偏多/偏空/中性）。
6. **连板/涨停情况速览** — 涨停/跌停数、连板数、最高板、上涨占比、连板梯队表、情绪判读。
7. **持仓诊断** — 每张持仓卡含现价/浮亏率/分时/均线位置/主力资金/板块共振 + 数据驱动点评（强弱/驱动/资金/技术/操作）。持仓由外部 `holdings.json` 提供（**不固化进 Skill**）。
8. **下午推演：风险点与机会点** — 风险/机会双栏清单（带高/中/低等级标签），末附"操作总纲"三条。

## 渲染与配色规范（已固化）
- 暗色主题：背景 `#0f172a`、面板 `#1e293b`、卡片 `#263348`。
- **中国股市配色**：涨红 `#ef4444` / 跌绿 `#22c55e` / 平 `#94a3b8`。class 为 `up/down/flat`。
- 所有图表为**内联 SVG**（分时 `spark()`、板块条形 `barchart()`），无 CDN/外部依赖。
- 响应式：`@media(max-width:640px)` 已处理手机端。
- 报告中**必须标注真实数据来源**（文末 `.src` 区块）——记录实际使用的接口，**不虚构 MCP/连接器状态**。
- **降级必须显式披露**：若 westock-mcp / tdx-connector 掉线而走了 hithink 或公开 API，报告内要用醒目提示写明「主数据源不可用，已降级至 X」，并列出降级后**哪些模块精度下降**（如：板块资金流缺失、概念板块只剩涨幅榜）。老板要靠这个判断结论可信度。

## 数据 schema（generate 脚本的输入契约）
合并 JSON（`midday_merged_{date}.json`）需含：
```
snapshot        dict[code] -> {code,name,price,prev_close,open,high,low,pct,amount_wan,...}
minutes         dict[code] -> [{t,p,v,amt}]   (仅 0930-1130)
kline           dict[code] -> [{date,open,close,high,low,vol}]  (>=70根日K)
sector_industry list[{name,code,pct,netflow,up,down,lead,lead_pct}]
sector_concept  list[同上]
zt_pool         list[{code,name,pct,first_time,open_times,streak,industry}]
dt_pool         list[{code,name,pct,industry}]
news            list[{title,time,media,summary,url}]
fundflow        dict[code] -> {main,small,mid,big,huge,time}  (元)
meta            {schema_version:2, report_date, collected_at, mode}
quality         {level: pass|warn|fail, errors[], warnings[]}   # 核心数据缺失→fail→采集器非零退出、不生成报告
sources         {minutes/breadth/news: {status, as_of, coverage?}}
```
涨跌家数：优先单独的 `breadth_{date}.json`，其次合并体内的 `breadth`。结构：
`{listed_total, valid_total, missing, up, down, flat, up_ratio_valid}`；
`up_ratio` 以 `valid_total`（剔除无报价/停牌）为分母，不直接用 listed_total。

## ⚠️ 先重试，再降级（v3.1，2026-09-01 血的教训 + v4.6 修正判据）
**MCP 掉线是「间歇性」的，不是「持续性」的。** 实测：12:05 采集时 westock-mcp + tdx-connector 同时断开，
13:01 westock **自己恢复**。本期因此白跑约 40 分钟探测 + 少了一个模块。

**正确顺序（务必遵守）：**
1. 先调一次目标 MCP 做连通性探测（用最轻量的工具，如 `data_quote` 只传 1 个代码）
2. 失败 → **等 3–5 分钟重试，最多 3 次**（大概率第 2 次就通）
3. 3 次仍失败 → 才降级，并在报告中写明"已重试 3 次仍不可用"
4. **降级后仍要回补**：报告发布后若有连接器恢复，用独立 patch 脚本回补缺失模块 + 做交叉复测
   （参考 `patch_midday_20260901.py`：正则从原报告 HTML 提取数值 → 与新源对比 → 追加「复测校验」章节）

### ⚠️⚠️ v4.6 重要修正：「ToolSearch 查不到 ≠ 工具未注册」（2026-09-14 实证）
此前自定的判据是"ToolSearch 按准确工具名查不到 = 工具没注册 → 重试无意义，直接降级"。
**这个判据是错的，2026-09-14 有反证：**

| 时刻 | 连接器状态 | 依据 |
|---|---|---|
| 09:20 | **可用** | 同工作区 AI×金融日报自动化当日成功调用 `westock-mcp.data_quote` |
| 12:01（本次采集） | **查不到** | 精确名 + 关键词检索皆无返回 → 当时判定"未注册"→ 直接降级 |
| 13:05 | **已恢复 connected** | 连接器状态面板显示已连接，`data_quote` / `data_sector` 调用成功 |

→ 说明 **12:01 那次只是"暂时未挂载/瞬时不响应"，不是"未注册"**。当时应重试而不是直接降级，
报告因此降到了第 4 级公开 API（损失板块资金流 5/20 日维度等）。

**修正后的判据（务必照做）：**
- ToolSearch 查不到 → **不要立刻下"未注册"结论**，先等 3–5 分钟重新探测，最多 3 次；
- 3 次都查不到，且**连接器状态面板也未显示该连接器为 connected**，才判"确实不可用"→ 降级；
- 若状态面板显示 connected 但工具检索不到，优先怀疑是本会话工具索引未刷新，
  **用 `DeferExecuteTool` 直接按全名调用试一次**（不依赖 ToolSearch 的检索结果）。

**v4.7 再修正：工具索引是「会话级固化」的 —— 中途重连不会回注本会话（2026-09-15 实证）**

| 时刻 | 状态面板 | ToolSearch | `DeferExecuteTool` 全名直调 |
|---|---|---|---|
| 12:01（采集时点） | 未显示 connected | 查不到 | `not found in the deferred tools index` |
| 12:08 | — | 查不到 | not found |
| 12:12 | — | 查不到 | not found |
| 13:29（复查） | **westock-mcp 已 connected** | **仍查不到** | **仍 not found** |

→ 结论：连接器**在会话开始后才连上时，本会话的工具索引不会回注**，此时 ToolSearch 与全名直调**双双失败**，
和"确实未挂载"表现完全一致。同日 09:20 的另一个会话（AI×金融日报）曾成功调用 westock `data_quote`，
说明可用性是**会话级**而非纯时间级。**因此：同一会话内反复重试价值有限（3 次足够），
换一个新会话重跑才是真正的恢复手段**；若已按降级链出报告，按既有规则在事后用独立 patch 脚本回补即可。

**v4.7 同时明确：同花顺「工具在册 ≠ 接口可用」（2026-09-15 实证）**
hithink-finance MCP 当日 `special_data_limit_up_pool` / `limit_down_pool`（涨停池/跌停池）**正常返回**，
但 `get_a_share_prices_snapshot`（A股版）、`get_a_share_index_prices_snapshot`（index 版）、
`get_a_share_index_prices_historical` **全部报**
`MCP error -32602: Structured content does not match the tool's output schema: data/data must be object`
（单代码亦复现）。→ **交叉校验要按接口能力分别登记成败，不能因为"连接器 connected"就假定指数校验已完成**；
报告文末的精度下降项里必须如实写明**哪一项校验没做成、原因是什么**。

---

## 数据源四级降级链（v3，2026-09-01 实战验证）
MCP 连接器**会整条掉线**（westock-mcp / tdx-connector 同时不可用发生过）。按序尝试，成功即用：

> **环境前提（2026-09-15 补）**：第 1–3 级依赖 **WorkBuddy 连接器**。
> **Codex 等无连接器环境不存在这三个选项，直接从第 4 级（公开 API）开始** ——
> 这在该环境下不是"降级"而是正常路径，两个脚本已内置、无需任何额外配置。
> 此时文末 `sources` 须记录「公开 API」为真实提供方，**不得谎称调用过 MCP**。

| 层级 | 数据源 | 覆盖 | 关键用法与坑 |
|---|---|---|---|
| 1 | **westock-mcp** | 全量，**含板块主力资金流（东财没有的）** | 首选。工具名 `mcp__westock-mcp__data_*`，直接调用参数会被序列化成 string 报 `Expected object` → **必须用 `DeferExecuteTool` 包装**。板块资金流：`data_sector(mode="ranking", kind="industry", type="mainNetInflow", order="desc")` 一次返回全部行业，字段 `mainNetInflow`/`mainNetInflow5d`/`mainNetInflow20d`/`upCount`/`leader`，**单位是万元** |
| 2 | **tdx-connector** | 全量 | `tdx_quotes` 必须传 `code` + `setcode`（0=深A/1=沪A/33=基金）；深层查询用 `tdx_security_deep_info` 的 `entity_type="A股代码\|港股代码\|美股代码\|基金代码\|指数代码"`（不支持「ETF代码」「A股指数」这类值）。超大结果自动落 `tool-results/` |
| 3 | **hithink-finance MCP（同花顺）** | A股/ETF/指数/行业指数，**不含可转债** | 见下方三条实测要点 |
| 4 | **公开 API** | 腾讯财经（`qt.gtimg.cn` 快照含**可转债**、`web.ifzq.gtimg.cn` 日K/分时）+ 东财（`push2`/`push2ex`） | 见「采集管线 + 硬坑」 |

### hithink-finance MCP 实测要点（v3 新增，务必保留）
1. **指数快照要用 A股版**：`get_a_share_prices_snapshot` 传 `000001.SH,399001.SZ,399006.SZ,...`。
   ⚠️ 传 index 版 `get_a_share_index_prices_snapshot` 且代码带 `.SH` → 报
   `Structured content does not match the tool's output schema: data/data must be object`。
2. **行业指数要用 index 版 + `.TI` 代码**：先 `get_a_share_index_catalog_ths_index_list(tag="industry")` 拿约 250 个 `.TI` 代码表，
   再批量喂给 `get_a_share_index_prices_snapshot`（一次约 30 个）。`.TI` 代码在该工具下**正常工作**。
   → 这是**拿真实领跌板块的最可靠途径**：东财板块榜只能拿到涨幅前 N，拿不到跌幅榜（见硬坑 3）。
3. **不含可转债**：传任意沪/深转债（如 `sh113050`/`sz128101`）报同样的 schema 错。可转债走腾讯 `qt.gtimg.cn/q=sh113050,sz128101`，
   返回 **GBK 编码**，需 `iconv -f GBK -t UTF-8`（或 Python `decode("gbk")`）。

## 采集管线（collect 脚本）+ 关键踩坑（务必保留）
数据源：腾讯财经 `qt.gtimg.cn`（快照）、`web.ifzq.gtimg.cn`（分时/日K）、东方财富 `push2`/`push2delay`（板块/涨跌家数/资金流）、`push2ex`（涨停/跌停池）、东方财富要闻流。
**三个硬坑（已固化进 collect 脚本，勿删）：**
1. **东财 push2 的 IPv6 路由不通 → 强制 IPv4**：`socket.getaddrinfo` 覆写为 `AF_INET`，否则 `HTTP 000`。
2. **`fs` 参数空格必须编码为 `+`**（用 `%20` 会被拒返回空响应）；用 `urllib.parse.quote(fs, safe="+")`。
3. **限流远比文档严重 → `pz<=90` 且只用第 1 页**：实测 `pz=200` 被拒、`pn>=2` 被拒、`po=0`（升序）被拒。
   → 后果：**东财板块榜拿不到真实领跌板块**（总共 496 个行业/概念，只能拿到涨幅前 90）。
   → 解法：**领跌板块改从 hithink 行业指数 `.TI` 全量拉取后在本地排序**（67 个行业指数足够覆盖），涨幅榜两者互为交叉验证。
   在 `push2.eastmoney.com` 与 `push2delay.eastmoney.com` 两 host 间兜底重试。
**涨跌家数**：用"全A逐页精确计数（pz=100）"，**不要用行业板块成分股求和**（跨层级重复计数不可靠）。
**分时**：`minute/query` 返回的是**当日**数据（与传入 date 无关），故采集脚本必须在**交易日当天 12:00 前后**运行，不能回补历史。

### Windows / 沙箱环境硬约束（v3 新增，踩过就别再踩）
1. **Python 直连 DNS 会失败**（`socket.gaierror: [Errno 11001] getaddrinfo failed`），而 `curl` 子进程度假正常解析。
   → **所有 HTTP 一律走 `subprocess.run(["curl","-s","-m","30",url,"-o",path])`**，不要 `urllib`/`requests`。
   → 建议封装带指数退避的重试（实测 5 次重试可救回大部分东财限流）：
   ```python
   def curl(url, name, tries=5):
       p = os.path.join(TMP, name)
       for i in range(tries):
           subprocess.run(["curl","-s","-m","30",url,"-o",p], check=False)
           try:
               with open(p, "r", encoding="utf-8") as f:
                   j = json.load(f)
               if j: return j
           except Exception: pass
           time.sleep(1.2 * (i + 1))
       print(f"[FAIL] {name}"); return None
   ```
2. **`/tmp` 不可写**（`No such file or directory`）→ 临时数据落工作区 `.tmpdata/`，用完清理。
3. **代理 127.0.0.1:7897 常被拒**（`WinError 10061`）且无 `HTTP_PROXY` 环境变量 → 不要依赖代理，直接 curl 直连反而通。
4. **长采集脚本会撞前台超时** → 直接 `run_in_background=true` 起，用 TaskOutput 取回，别反复 sleep 轮询。

## 运行方式
```bash
# 1) 采集（交易日当天运行；产出 midday_merged_{date}.json + breadth_{date}.json）
python collect_midday_data.py --date 2026-08-21

# 2) 渲染（date 决定输出文件名 午间复盘_{date}.html）
python generate_midday_review.py --date 2026-08-21 \
    --merged midday_merged_20260821.json --breadth breadth_20260821.json

# 3) 交付（按当前环境能力：WorkBuddy 用 present_files + cwd；其他环境各自交付）
```

### 运行模式（时间一致性，v2 新增）
采集器 `--mode` 明确三态，缺失时按当前时刻自动判定（11:30–13:00→`strict-midday`，其余→`late-snapshot`）：
- **`strict-midday`**（默认，仅 11:30–13:00 允许采集）：全模块以上午 11:30 为统一基准。
- **`late-snapshot`**（13:00 后运行）：仅指数"上午收"还原至 11:30；板块/广度/资金流/涨跌停/资讯标为**「当前快照」**，严禁叫"上午"。
- **`render-archive`**：用中午已保存的 `midday_merged_{date}.json` 直接重渲，是**唯一可信的历史补跑**方式。

### 数据质量闸门（v2 新增）
采集器产出 `meta(schema_version=2/mode/collected_at)` + `quality(level/errors/warnings)` + `sources(status/as_of)`。
最低校验：`--date` 必须等于采集当日；7 指数齐全且分钟末点 11:30；`up+down+flat+missing==listed_total`；
资讯属报告当日且不晚于 11:30（strict 模式）。**核心数据缺失→非零退出且不生成报告**；可选数据缺失→标注"数据不可用"而非 0。

**v4.6 新增「行情日期一致性」校验（重要）**：腾讯分时接口在 `data` 节点下自带 `date` 字段（如 `"20260914"`）。
采集器现在把它记入 `QUOTE_DATES`，并在质量闸门中与报告日比对，**不一致即判 `fail` 并阻断出报告**。
原因：`--date == 今日` 这道闸门只挡得住"把今天的数据写成历史日期"，**挡不住反方向**——
周末/节假日运行、或接口异常时，DATE==今日 照样通过，但接口返回的是**上一交易日**的分时/快照
→ 报告会被标成今天却使用了前一天数据（这是降级到公开 API 后最现实的"数据错"风险）。
另：原「分时末点非11:30」判据 `ms[-1]["t"] > "1130"` 在数据已截断到 ≤1130 的前提下恒不成立（死代码），
已改为校验 strict-midday 模式下末点必须正好是 `1130`。

## 事实层与条件叙事（v2 新增，P2）
所有结论句必须由 `compute_facts()` 推导的**事实层**驱动，禁止无条件硬编码。事实层字段（generate 脚本内）：
`breadth_regime`（<40弱/40-55中性/>55强）、`volume_regime`（推算全日量能<100缩量/100-115持平/>115放量）、
`index_pattern`（冲高回落/探低回升/单边）、`leader_concentration`（龙头资金占比>=40%为高集中，相关性）、
`news_confidence`、`zt_regime`、`weekday`（仅真实周五才提示"周末窗口"）、`ff_rel_rank`（板块资金流**排名**而非绝对额）。

**叙事红线（务必遵守）：**
- 每句判断可追溯到：用了什么指标、阈值多少、数据缺失时是否停判（`missing`→不输出该结论）、属于 fact/correlation/speculation。
- 去因果化：不用"核心原因""直接压制""派发、散户接盘""下方空间有限""确定性最高"等因果/承诺式措辞；改"同时出现""可能反映""需进一步验证""盘中强度居前""相关性"。
- 节奏词不硬编码："周五效应"严禁；"周末窗口"仅在 `weekday==4` 时由数据触发。"平开后/高开后/低开后"按开盘涨跌幅条件选择。"距成本"按盈亏分档（深套/小幅低于/接近/扭亏）。
- 资金流相对强弱用**排名/分位数**，避免"净流入 10 亿"类绝对阈值对不同体量板块不公平。

## 配置点
- `IDX`（指数）：7 大指数 `(code, 名称)`，**已设为合理默认值**，一般无需改。
- **持仓（重要）：持仓是【个人私有配置】，刻意不固化进本 Skill**，避免把真实持仓写进可分享模板（含 WorkBuddy/Codex 通用模板）。
  - 提供方式（任选其一）：
    1. 在运行目录或本 skill 的 `scripts/` 同级放置 `holdings.json`（自动加载）；
    2. 运行脚本时传 `--holdings 路径.json`（采集与渲染两个脚本都支持）。
  - 格式见同目录 `holdings.example.json`：
    ```json
    {
      "holdings": [["sh600519", "贵州茅台", 1500.00], ["sz159915", "创业板ETF", null]],
      "hold_ctx": {"sh600519": {"sector": "白酒", "note": "白酒龙头"},
                   "sz159915": {"sector": null, "note": "宽基ETF"}}
    }
    ```
  - 未提供时，报告"持仓诊断"模块显示占位提示，其余模块照常生成。
  - `holdings` 每行：`(代码, 名称, 成本)`，成本未知填 `null`/`None`（报告自动标注"成本未记录"并跳过浮亏率）。
  - 代码前缀：`sh`/`sz` + 6 位。**可转债同样支持**（如 `sh113050`、`sz128101`），走腾讯 `qt.gtimg.cn` 取价；沪市可转债代码段为 `113xxx`/`110xxx`/`111xxx`，深市为 `128xxx`/`127xxx`/`123xxx`。
  - `hold_ctx`：每只持仓关联的"所属板块"名称（用于共振判断，如 `"钨"`/`"半导体"`；ETF/宽基填 `null`）。
  - `.gitignore` 已忽略 `holdings.json`，切勿误提交真实持仓。

## 叙事精修（可选）
报告所有文案默认**由数据驱动自动生成**（保证自动化无人值守可跑）。若某天想注入人工精修的高质量文案，
可准备一个 JSON（`--narrative path.json`），键名：`s01/s02/s03/s05/s06/s07`（模块点评）与 `s08_risk/s08_opp`（下午推演两栏）。
提供即覆盖，不提供则自动生成。

## 输出与推送约定
- 报告写入 `daily-review/午间复盘_{date}.html`。
- 交付：使用**当前环境可用的文件交付能力**（WorkBuddy 用 `present_files` 并设 `cwd`；Codex 等其他环境按各自能力交付，不硬编码单一工具）。
- 时间一致性：顶部 `.warn` 框按运行模式统一声明口径——`strict-midday` 全模块为上午基准；`late-snapshot` 仅指数"上午收"还原至 11:30，板块/广度/资讯/涨跌停标为"当前快照"。**禁止把午后数据标成"上午"**。

## v4 输出缺陷清单（2026-09-02 实测，全部已修进脚本，勿重犯）

生成后**必须扫一遍这几处**，它们都是模板里"换了行情就露馅"的硬编码：

| # | 症状 | 根因 | 修正 |
|---|---|---|---|
| 1 | 证券名称全乱码（`\ufffd\ufffd\u05a4…`） | `get()` 默认 `decode="utf-8"` 解腾讯 GBK，因 `errors="replace"` 不抛异常而静默损坏 | `collect_snapshot()` 显式 `decode="gbk"`；`get()` 增加编码自适应（严格 UTF-8 失败或出现 U+FFFD → 退回 GBK） |
| 2 | 走势叙事把「低开→探底→回升」写成「**冲高**后回落」 | 只判断 `hi_first`（高点在低点前），没看收盘相对低点的回升 | 新增 `bounce = am_pct - lo_pct`，形成三分支：`bounce>0.3` → 低开探底回升；否则才叫冲高回落 |
| 3 | 领涨 TOP5 被「地面兵装Ⅲ / 地面兵装Ⅱ」两条重复占位 | 东财行业板块含 Ⅰ/Ⅱ/Ⅲ 多级同名录入 | `_dedup_sectors()`：去罗马数字后缀后重名 + 涨跌幅/净额/涨跌家数/龙头全一致 → 只留一个 |
| 4 | 持仓「主力净流入 +720 万」却给「主力净流出背景下不宜加仓」 | 操作建议的 `else` 分支写死"净流出"文案 | 拆成净流入 / 净流出 / 数据缺失三支 |
| 5 | 「创业板指**涨 -2.18%**」语病；量价结论写死「指数涨、个股跌」 | 文案硬编码 | `build_divergence_note()` 按创业板指实际方向三分支，跌时用 `abs()` |
| 6 | 「核心事件：上午无重大宏观催化」但表里 14 条资讯 | `CORE_KEY` 只认恒大/临港/以旧换新等某日特定事件 | 泛化为央行/财政/统计/监管/关税等通用词；无命中时改述"未出现央行/财政/统计口径的重大事件" |
| 7 | 「指数红、账户绿」「指数与个股**严重背离**」出现在指数同跌日 | 未判断指数方向 | 仅在 `上证 am_pct >= 0` 时才用"指数红"表述，否则说"同步走弱" |
| 8 | 全 7 指数收跌却写「上证50 **领涨**」 | 同上 | `strong_word = "相对抗跌" if strong.am_pct < 0 else "领涨"` |
| 9 | 科创50 明明 V 型修复却说"日内回吐" | 只看 `retrace` | 增加 `hi_t > lo_t 且 bounce>0.8` → 描述为"探底回升、日内动能最强" |
| 10 | 资讯只筛出 3 条 | `KEY` / `IMPACT` 只覆盖某一天的事件词 | 重写为七类通用词表（货币/财政产业/宏观数据/监管市场/行业主题/公司业绩/外部），`IMPACT` 具体词排在泛化词之前 |

**同一类坑的第二批（2026-08-25 回归测试发现，均已修）：**

| # | 症状（出现在"指数跌但个股普涨"日） | 修正 |
|---|---|---|
| 11 | 7 指数全绿却输出"指数与多数个股**同向走强**，行情具备一定广度支撑" | 宽度联动改为**「指数方向 × 上涨占比」二维判定**：`上证<-0.1 且 占比过半` → "个股普涨而指数承压，权重股拖累、中小盘活跃" |
| 12 | "量价同步走弱……**仅 66%** 个股上涨，指数与个股同向走弱" | ①"仅/有"由占比阈值切换（<45% 才用"仅"）；②新增第四分支：`指数跌 且 占比>=55%` → **权重拖累型分化**，明确"不应按普跌市处理" |
| 13 | 风险点"市场宽度偏弱，指数与个股**同步下行**。仅 66.4% 个股上涨" | 二维判定：占比>=55% → 降级为"中"风险，改写为"指数承压而个股普涨，存在权重股补跌拖累指数的风险" |
| 14 | 操作总纲"其余在 **66%** 上涨占比环境下追高胜率低" | 占比>=45% → 切换为"轻指数、重个股"表述 |
| 15 | "盘中**冲高 -0.10%**"（高点本身仍是负的） | `hi_pct < 0` 时改述"盘中最高仅 -0.10%" |

**第三批（2026-09-04 实测发现，形态 = 「指数红 × 个股普涨」，即四态夹具外的第 5 形态，均已修）：**

| # | 症状（出现在"指数红 + 普涨共振"日） | 修正 |
|---|---|---|
| 16 | 74.5% 普涨日模块 08 却报「市场宽度偏弱，指数红、账户绿」（上证 +0.35% 收红） | 宽度风险条改为**二维四象限**：指数红×普涨(≥55) = 共振健康 → **该条整体跳过**；指数平/跌×普涨 → 权重拖累表述（缺陷13）；指数红×宽度不足(<55) → "指数偏强而宽度不足/指数红账户绿"仅此象限成立；其余 → 同步承压。等级按 `up_ratio<40` 高 / 其余中（原 <55 一刀切"高"过重） |
| 17 | 模块 08 机会条（净流入第一概念）写「但指数尚未收复昨收」，当日上证已收红 | else 分支写死"未收复昨收"。三态化：冲高回落形态(hi先且retrace>0.8)→"高位分歧"；上证收跌→"仍收跌 X%、主线能否带动修复待验证"；收涨→"指数已收复昨收、该方向资金靠前；需防一致性过高后的短线分歧" |
| 18 | 模块 02 主线资金句「上涨由少数主线拉动而非全面行情」出现在 74.5% 普涨日 | 无条件硬编码 → 按 up_ratio 三态：<45 少数主线拉动 / 45–55 主线与个股分化并存 / ≥55 "个股普涨、赚钱效应较广，资金主线为其中强度居前的方向" |
| 19 | 模块 02 探底回升段「但仍未收复昨收（距平盘 X pct）」在 V 型翻红日矛盾 | cp<0 → 未收复表述；cp≥0 → "并已收复昨收、由跌转涨" |
| 20 | 模块 01「开盘分化」：0 低开 7 高开仍判分化 | 只有低开档位。对称补高开：n_up≥5 全线高开 / n_up≥4 多数高开 / n_down≥5 全线低开 / n_down≥4 多数低开 / 其余开盘分化 |

**教训（2026-09-04）**：#16-20 全部是「换个行情形态就露馅」的硬编码，且四态夹具均不覆盖"指数红×普涨"象限，当日只测今日数据全绿也无法发现。
→ 修完 #16 后补了第 5 态夹具 `bull_resonance_merged.json`（weight_drag 派生、唯一变量=指数方向翻红），
`run_three_state.py` 增加**负断言**能力（禁止文案清单），对该形态断言"市场宽度偏弱 / 指数红账户绿 / 权重拖累"必不出现。

**第四批（2026-09-08 实测发现，形态 = 「指数平 × 个股普涨」，缺陷 #22 残留 + 量能措辞硬编码，均已修）：**

| # | 症状（出现在"创业板指平收±0.3% 内 + 62% 个股普涨"日） | 修正 |
|---|---|---|
| 21 | 量能措辞无条件硬编码：115%（前5日均量，flat 档上沿）仍写"上午量能偏弱""基本持平至小幅缩量"；模块 08 风险条亦写死"量能偏弱" | 模块 04/08 按 `volume_regime` 三态化措辞（shrunk→偏弱缩量 / flat→基本持平 / expanded→温和放大），新增 `_VOL_LAB/_VOL_TAIL/_VOL_RISK` 变量 |
| 22 | 模块 04 divergence 句平收分支（`cyb` 在 ±0.3% 内）无脑输出"指数平、**个股弱**、量能平"观望形态——62% 上涨日与"有 62% 个股上涨"自相矛盾（9/7 缺陷 #22 登记后残留） | 平收分支按 `up_ratio` 二维化：≥55 → "指数平、**个股涨**、量能平"整理形态（个股活跃度不弱、权重拖累指数横盘，不宜按弱势市处理）；<55 才落"个股弱"观望形态 |

**教训（2026-09-08）**：#21/#22 都是「平收/临界档位」硬编码，且第五态夹具 `bull_resonance` 的指数 +0.4% 走的是 `cyb>=0.3` 分支，**测不到平收分支**（当日五态回归全绿 + 今日数据全绿仍复现）。
→ 修完补了**第 6 态夹具 `index_flat_bull_merged.json`**（weight_drag 派生、`flip_index_down` 支持 `per_code` 逐指数平移：创业板指 -0.15% 触发平收分支、其余指数 ±0.2% 平盘、广度保持 66%），
对"个股弱、量能平 / 观望形态"做负断言；`flip_index_down` 现支持 `per_code` 参数供逐指数目标涨跌幅。

**第五批（2026-09-09 实测发现，缺陷 #23：量价提示与量能档脱钩 + 调用点早于事实层）：**

| # | 症状 | 修正 |
|---|---|---|
| 23 | 模块 04 `build_divergence_note()` **所有分支量能措辞写死**——117% 放量日（volume_regime=expanded）仍输出"量能亦未放大 / 普跌缩量形态 / 量能平"，与模块 04 量能结论"温和放大 117%"直接矛盾；且该函数在 1010 行被赋值、**早于 FACTS（1017）定义**，根本无法引用量能档 | ① `DIVERG_NOTE` 赋值点后移至 `_VOL_*` 量能档措辞定义之后；② 函数内所有量能措辞按 `_VREG` 三态化（缩量/持平/温和放量 + 对应长句与尾句：expanded 档尾句不再写"量能仍不能放出"）；③ 顺带修复涨分支 cyb>=0.3 在 up_ratio>=55 普涨日仍写"个股跌"的矛盾 → 补 up_ratio 三档（≥55 量价齐升普涨 / 45–55 指数涨个股分化 / <45 结构性背离），平收与跌分支维持已有二维判定 |


**第七批（2026-09-11 实测发现，缺陷 #25/#26/#27：最高价=开盘价时的"冲高"误判 + 持仓"最弱"标签重复 + 风险条板块选择错位）：**

| # | 症状 | 修正 |
|---|---|---|
| 25 | 普跌日**4 只持仓同时被标"上午最弱持仓"**（互斥最高级却重复出现，且 -1.79% 的某通信主题ETF 与 -4.50% 的示例个股A 同级） | 根因：`build_hold_note` 按固定阈值 `am_pct<-1.5` 就地打标，无组合内相对排名。改为在持仓卡循环前**按组合内 am_pct 统一排名分配** `_HLAB`：仅最弱一只标"上午组合内最弱"、最强一只标"上午组合内最强"，其余按 ±1.5% 分"走强/走弱/震荡持仓"；`build_hold_note` 新增 `label` 参数（不传则退回旧绝对阈值表述） |
| 26 | **"最高价即为开盘价、全天未上行"却写"冲高/带动指数快速拉升"**——当日上证开 -0.60% 即为全天最高（-0.60%），此后单边下行至 -2.08%；模块 02 早盘段仍写"资金净流入居前的板块带动指数快速拉升"、盘中段写"冲高后震荡回落"，模块 04 量能结论写"与指数冲高回落同时出现"，模块 07 持仓写"低开后冲高回落"，模块 08 风险条写"科创50 冲高回落"、机会条写"指数自高点回落…高位分歧" | 根因：`hi_first`（高点在低点之前）只说明**时序**，不等于**曾经上行**。统一补第二个判据 `_lift = hi_pct - open_pct`（自开盘价上行幅度），**`_lift >= 0.15` 才允许"冲高/拉升/高位分歧"，否则改述"开盘即高点/单边下行"**。落点 5 处：① 模块 02 elif 分支（`_lift<0.15` → 只出"盘中/单边下行"一块，不再编"冲高"块）；② `index_pattern` 分类新增 `open_high_fall="开盘即高点后单边下行"`；③ 模块 04 `_PAT_CN` 同步补该键；④ 模块 07 `shape`（`retrace>0.8` 时按 `lift` 切"冲高回落"/"单边回落"）；⑤ 模块 08 高位回落风险条与机会条 `_div` |
| 27 | 风险条"**主力净流出的板块对相关持仓形成压制**"却选中"橡胶助剂 -0.0亿（1涨1跌）"这类**几乎持平、与持仓毫无关系的冷门板块** | 根因：`ind_v` 按涨幅降序，原写法 `[r for r in ind_v if persist(r)[0]<=0 and netflow<0][:2]` 取的是**列表前两个**（即最接近涨跌幅榜顶部、跌幅最小的），而非净流出最大者。改为按 `netflow` 升序取前 2（净流出最显著方向），并显式标注是否与持仓所属板块重叠（当日正确命中 电子 -199.9亿 / 半导体 -104.4亿，其中半导体为持仓所属方向） |

**教训（2026-09-11）**：#26 是 #15（"盘中冲高 -0.10%"）与 #24（"跌幅收敛并翻红"）的**同一族第三次复发**——
**凡断言方向动词（冲高/拉升/翻红/收复/高位分歧），必须同时校验"相对基准点是否真的朝该方向动过"**。
`hi_first`/`retrace`/`bounce` 都只描述时序与幅度差，**不含"相对开盘价是否上行"这一信息**；此前夹具的
`hi_first=True` 形态里最高点均高于开盘价，故 #26 在八态全绿下仍复现。
→ 补**第 9 态夹具 `open_high_selloff_merged.json`**（2026-09-11 真实采集数据脱敏：剔除 6 只真实持仓，
保留 7 大指数 + 板块/广度/涨跌停/资讯；形态=全线低开、**上证开盘 -0.60% 即全天最高**、低点 10:44 -2.08%、收 -1.82%，
广度 6.3% 极端普跌（350涨/5180跌）、量能 expanded 档 136%），负断言
`{"带动指数快速拉升", "冲高后震荡回落", "与指数冲高回落同时出现", "指数自高点回落，显示高位分歧"}` 必不出现。
**注意**：同一日创业板指自开盘上行 0.46pct，其"冲高回落"属真实表述，故负断言只针对上证口径文案——
**夹具负断言必须精确到"该形态下真正不该出现的句子"，不能按关键词全域封杀。** tests 升级**九态**。

**第八批（2026-09-14 实测发现，缺陷 #28/#29/#30/#32/#33：第 10 形态「指数内部方向分化 × 个股普涨」）：**

| # | 症状 | 修正 |
|---|---|---|
| 28 | **同一天报告自相矛盾**：模块 01 输出"指数与多数个股**同向走强**"、模块 06 输出"指数与个股方向**大体一致**"，模块 04 却输出"指数与个股方向**相反**、权重拖累"——因为 M01/M06 只看**上证**（+0.16%），M04 看**创业板**（-0.47%） | 新增「指数内部分歧度」维度：7 大指数中 **≥2 只涨 且 ≥2 只跌** 即判"指数内部方向分化"，落点 3 处（M01 宽度联动新增 `_split` 分支、M06 `_rel` 判定、M04 量价涨分支 `_rel2`）。既有夹具不受影响（weight_drag=7 指数全跌、bull_resonance=全涨、index_flat_bull 仅 1 只跌，均不满足 split 条件） |
| 29 | 模块 01「为全场**最弱/最强**的日内动能表现」是**硬编码最高级、未做跨指数排名**——科创50 自低点修复 1.38pct 被称"全场日内动能最强"，实际中证1000 修复 1.82pct 更强（科创50 仅第 2） | 按修复幅度（`am_pct-lo_pct`）与回落幅度（`retrace`）跨 7 指数排序：第 1 名才写"为全场最大"；其余写"居全场第 N（最大为 XXX 的 Y.YY 个百分点）" |
| 30 | 模块 02 `hi_first=False` 分支**整句写死**「部分板块发力，指数反弹冲高」「但量能未能跟进，尾盘再度回落」，不引用任何数据 → **实测 6 个交易日中 4 天走本分支却输出逐字相同的句子**（retrace 0.10 / 0.17 / 0.29 / **0.01** 全部写"尾盘再度回落"）；尾盘句「窄幅整理收官」+「多空分歧显著」同样 6 天全中（含极端普跌 -1.82% 日）；且拼接出 `。。` 双句号 | 中段按 `hi`（是否翻正）+ `retrace` 三档条件化（≤0.15 持稳 / ≥0.6 明显回落 / 其余小幅回落）；尾盘句改数据驱动（`retrace≤0.05` → "高位持稳收官"，并按 retrace/bounce 选择后续句），删掉写死的"多空分歧显著"，重排拼接消除 `。。` |
| 32 | 操作总纲② 写死「科创50 `<昨收>`（昨收，破位则成长股离场）」——当日科创50 上午收 1538.84 **已低于昨收 0.94%，"破位"其实早已触发**，仍按"待破位"表述会误导 | 新增状态校验：未破位才用昨收表述；已破位改写为「上午低点；现价 X 已低于昨收 Y，再破上午低点则走弱信号强化」 |
| 33 | 风险条「净流出板块与持仓方向重叠」判定**只按 `hold_ctx.sector` 精确匹配**——示例 ETF（`sh11xxxx`「某通信主题ETF」）sector 为 `null` → 「通信 -52.0亿」未判重叠，报告写"与持仓所属方向无直接重叠"，但该 ETF 恰为当日组合内最弱（-2.07%） | 从持仓名称剥离 ETF / 基金公司 / 转债 等后缀取关键词（"某通信主题ETF"→"通信"）后双向包含匹配，与 `hold_ctx.sector` 一并入关键词集合 |

**教训（2026-09-14）**：本期是**第三条同族铁律**——
① **凡"最高级"表述（最强/最弱/最大/唯一）必须由跨样本排序得出**，不能按分支语义想当然
（与 #15/#24/#26 的"方向动词必须校验是否真的动过"同源）；
② **硬编码的影响面要用多日实测量化**——正是用 6 个历史交易日跑同一分支，才看清"4/6 天输出同一句"
而非偶发，这是判断优先级的关键证据；
③ **夹具的"期望文案"也可能是错的**——`broad_rise` 夹具的旧断言"行情具备一定广度支撑"
本身就是错的那句，却因长期绿灯被固化了 9 个版本；
修脚本时必须同步修断言，否则"全绿"会变成错误行为的保护伞。
（**2026-09-15 补充更正**：该夹具按**渲染真实口径 `am_pct`** 复算后确认为 **4 涨 2 跌 1 平**，
此前"4 跌 3 涨"的说法是按夹具 `snapshot.pct` 误读所致，`snapshot.pct` 与 `am_pct` 两套口径并不一致——
详见「第九批」开头的证据方法与 `tests/run_three_state.py` 顶部提示。）

**第十一批（2026-09-17 实测发现，第 13 形态「广度中性下沿 × 低开冲高回落」批次 2 处：缺陷 #45/#46）：**

| # | 症状 | 修正 |
|---|---|---|
| 45 | 模块 02 **开盘段**对低开日**无条件**写「开盘后短暂下探至上午最低 X.XX（x.xx%）」——**未校验低点出现时刻**。2026-09-17 上证低开 -0.38%，上午低点 3868.10（-0.60%）**迟至 10:14** 才出现（09:34 反而先创出 +0.18% 的上午高点）→ 本句把 10:14 的盘中低点**错记成开盘时段**，且与同一模块紧随其后的「早盘 09:34 冲高 / 盘中 10:14 回落」**直接矛盾**。同族问题在**高开侧对称存在**：「开盘后短暂冲高至上午最高」在最高点出现于盘中时同样不成立（本次一并修） | 开盘段改为按**极值实际出现时刻**判定：新增 `OPEN_END = "0940"`（与开盘段标题 09:30–09:40 对齐）；`hi == op` → "开盘即为上午最高点…其后单边下行"（不变）；低开且 `low == open` → "开盘即为上午最低…此后未再跌破该位置"；低开且 `lot <= 0940` → 保留"开盘后短暂下探至上午最低"；**低点迟到时** → "开盘后弱势震荡，**上午低点迟至 HH:MM 才出现**（X.XX，x.xx%），**并非开盘时段形成**"；高开侧同构（`hit <= 0940` 才用"短暂冲高"，否则"开盘后高位震荡，**上午高点迟至 HH:MM 才出现**…并非开盘时段形成"） |
| 46 | 模块 01 的 `breadth_regime == "neutral"` 分支（40% ≤ up_ratio < 55%）**一档到底**，无条件写「指数与个股方向一致、**涨跌大致相当**，**行情结构偏均衡**，**主线与补涨并存**」。但**全库其余 9 处广度判定一律在 45 分档**（L172/208/705/837/840/845/848/851/890），**唯独此处**把 40–45% 也算进"均衡"。2026-09-17 up_ratio = **40.7%**（2260 涨 / 3102 跌）已贴弱档下沿 → 本句与同页 M06「指数与个股同步走弱，缺少逆势赚钱效应」、M02「主线与个股表现分化并存，普涨面一般」**口径互斥** | neutral 内**以 45 再分两档**，并与 `idx_dir()` 的指数方向（`_split` / `_trend`）组合：`up_ratio < 45` → "…；但从个股看**下跌家数多于上涨**——**仅** N 只上涨（x.x%，不足五成）、M 只下跌，**结构偏弱**，上涨集中于少数主线，普涨面有限，**不宜按均衡市处理**"；`>= 45` → 保留原"…；个股层面涨跌大致相当——…行情结构偏均衡，主线与补涨并存"。方向前缀由 `_trend` 四态给出（内部分化 / 维持在正收益区间 / 普遍收跌 / 大体走平），四个分支共用 `idx_dir()` 单一事实源 |

**教训（2026-09-17 第二批）**：

→ **#45 是 #39 同族的第二例**，二者都是**时段动词未接实际时刻**：`#39` 错在**尾盘句**（"全天未再跌破…"），
`#45` 错在**开盘句**（"开盘后短暂下探至上午最低…"）。v4.8 只修了尾盘句、**开盘句未同步修复，隔一天就复发**。
→ **推论：修一处同族缺陷时必须横向 grep 全部时段/位置断言（开盘段 / 盘中 / 尾盘 / 全天）并逐句校验，
而不是只修被报告点名的那一句。** 本次横查覆盖 `开盘后` / `开盘即` / `短暂` / `早盘` / `探底` /
`冲高至` / `下探至` / `跌回` / `跌破` 九个模式，确认开盘段是唯一残留点（尾盘句已在 v4.8 条件化；
`open_high_fall` 的"开盘即高点"分支与 `_lift` 判定此前已按 #26 修好）。
→ **#45 还揭示"分支语义 ≠ 时间事实"**：`op < 0`（低开）只是**开盘方向**，**不蕴含**"上午最低点发生在开盘时段"。
凡文案里出现时段名词（开盘/早盘/盘中/尾盘/全天），其后的动词都必须在**该时段内**有数据支撑（这里即 `lo_t` / `hi_t`）。
→ **#46 同样揭示"阈值不统一"这类隐形缺陷**：9 处用 45 分档、1 处用 55，平时不报错，
只在**边界样本**（40.7% 这种贴着下沿的值）上暴露成跨模块互斥。
修法不是"就地补一个 if"，而是**把该维度的分档收敛到一处**——#46 已改为复用 `_trend` 单一事实源、
阈值统一到 45；把分档整体收敛进 `compute_facts()` 的 `breadth_regime` 是后续可选项。
→ 回归盲区：`differentiation` 夹具（46%）**一直覆盖 neutral 分支**，却因只断言"行情结构偏均衡"
——**恰好就是本缺陷的错句**——而长期绿灯，与 `broad_rise` 固化错句的旧例完全同型。
**再次印证 v4.7 教训③：断言写错比没有断言更危险。**
→ 补**第 13 态夹具 `neutral_edge_fade_merged.json`**（2026-09-17 真实采集数据脱敏——剔除全部 6 只真实持仓，
保留 7 大指数 + 板块/广度/涨跌停/资讯；形态 = 7 大指数**全线低开**、盘中翻红后回落收跌 + 广度 **40.7%**
落 neutral 偏弱下沿 + 上午低点**迟至 10:14**），正断言 6 条 / 负断言 4 条。
该条同时是**跨模块口径一致性**的回归锁：M01 与 M06 的断言必须**同页共存而不互斥**。tests 升级**十三态**。

**第十批（2026-09-17 实测发现，第 12 形态「科技成长主导的普涨共振」批次 7 处：缺陷 #38/#39/#40 + 工具链 #41/#42/#43/#44）：**

| # | 症状 | 修正 |
|---|---|---|
| 38 | 模块 04 `build_divergence_note()` 的结论尾句（非放量档）**无条件写死**「**指数缺乏向上突破的动能**，下午若量能仍不能放出，高位品种回落风险大于上行空间」，而该 `tail` 被**全部 5 个方向分支共用**（含 `cyb>=0.3` 的上涨分支）。2026-09-16 实测：创业板指 +2.35%、科创50 +4.50%、上证自日内高点仅回落 0.01pct 收在高位，同函数 head/body 已写"量价齐升、有 76% 个股上涨、赚钱效应较广"，**尾句却仍作方向性否定**，同一段内自相矛盾 | `tail` 按「指数方向（`cyb` 三态：涨≥0.3 / 跌≤-0.3 / 平）× 量能档（`_VREG` 二态）」二维化共 6 种措辞。上涨档改为"不过量能未同步放大，下午需以补量确认上行——若午后成交无法跟进，则上行空间或受限"（放量档为"…需关注午后能否延续"），**上涨档一律不得再出现方向性否定判断**；下跌档保留"缺乏向上突破的动能"（该形态下成立）、平收档改中性表述 |
| 39 | 模块 02 尾盘句在 `retrace<=0.05` 时写死「**全天未再跌破开盘低点**，下午关注量能能否跟进」——但"上午低点高于开盘价"**并非必然成立**（2026-09-16 上午低点 3843.28 **低于**开盘价 3861.75，10:01 触及），该句与事实直接矛盾 | 按「最低价 vs 开盘价」两态 + 日内低点出现时间条件化：`low >= open` → "全天未跌破开盘价"；低点在 09:35 之前 → "开盘后短暂下探至上午低点 X.XX（x.xx%）后回升，此后未再跌回该位置"；其余 → "开盘后于 **HH:MM** 下探 X.XX 形成上午低点，此后未再回落至该点位附近" |
| 40 | `tests/build_fixtures.py` 的 `--real` 模式因**对象别名（aliasing）**写出错误元数据：`coerce_base(d)` 会**原地**把 `d["meta"]` 重置为硬编码基底日期 `2026-08-21 11:30:00`，而它收到的 `d` **就是** `real_fixture()` 里的 `base` 本身；旧代码把 `src_meta = base.get("meta")` 写在 `coerce_base(base)` **之后**，等于读自己刚被覆盖掉的值 → 所有 `--real` 夹具的 `meta.report_date` / `collected_at` 长期写成 2026-08-21（`low_open_recover` 源 09-10、`index_split_bear` 源 09-15、`broad_tech_surge` 源 09-16 **全部中招**），直接违背本 skill 的 trust 标签「时间一致性 / 数据可审计」 | `src_meta` 取值**前移到 `coerce_base()` 之前**（并加注释警示该函数会原地重置 meta）；3 份受影响夹具用修复后脚本重新生成，已逐字段核验**除 meta 日期外完全等价**、无其他漂移 |
| 41 | `tools/deploy_to_github.py` 的 `api()` **只在 DNS 失败（`getaddrinfo`）时**降级 curl 后端，对**超时 / 连接重置**（WinError 10060 等）直接抛异常向上冒泡 —— 而本机（家宽 / 沙箱）对 `api.github.com` 是**间歇可达**，实测本次发布**第 1 次尝试即在中间文件上整脚本 abort**（26 个文件跑到一半 Traceback 退出），必须人工重跑 | `api()` 加**指数退避重试**（`tries=4`，1.5s×n）+ 连接类异常同样降级 curl 后端（curl 走独立 TCP 栈，抖动时往往能通）；抽出 `_RETRY_MARKS` 判定，**只重试网络类异常**——已拿到 HTTP 状态码（含 4xx/5xx）一律直接返回，既不重试也不掩盖权限等问题 |
| 42 | `main()` 的逐文件上传循环中，任一文件失败只 `print("[FAIL] …")` 便**继续执行**，末尾照样打印"完成"并以**退出码 0** 结束 —— 调用方无法从退出码分辨"全部成功"与"部分失败"（**假成功**） | 循环内已 `try/except` 包裹并收集 `failed` 列表；末尾若有失败则打印失败明细 + **`sys.exit(1)`**，并提示"逐文件 PUT 幂等，重跑即可"；仅全部成功时才打印 `完成（N 个文件全部上传成功）` |
| 43 | `tools/deploy_to_github.py` **只有 PUT、没有任何 DELETE 语义** —— 因此"本地删了"**从不等于**"远端删了"。实测证据：`tests/breadth_20260824.json`（`--selftest` 残留）本机已于 2026-09-15 删除、且已加入 `SKIP_GLOBS` 不再上传，但远端 2026-09-01 那次发布留下的副本**至今仍在**，形成"本地零残留、远端有残留"的假象（全量隐私复扫才发现）。该字段类缺陷渲染与回归均不可见 | 新增 `remote_manifest()`（取远端 tree，失败**返回原因而非抛异常**）、`find_orphans()`（纯函数：远端有而本地清单无 = 孤儿）、`delete_remote_file()`（先 GET sha 再 DELETE，404 视为幂等成功）、`list_or_prune()`（统一入口，返回退出码）；新增 `--list-orphans`（只读报告）与 `--prune-orphans`（**显式**删除，默认关闭）与 `--keep-remote`（追加豁免通配）。**三重安全**：① 孤儿处理**只在上传全部成功之后**执行——中途失败时远端处于半态，"远端有而本地无"会把刚失败的文件也误判为孤儿；② `KEEP_REMOTE` 白名单豁免 `LICENSE`/`NOTICE`/`.github/**`/`CHANGELOG.md`，避免误删仅在网页端维护的文件；③ 取不到远端清单时**绝不继续**，`--prune-orphans` 下直接判失败 |
| 44 | `api()` / `_api_curl()` 使用 `urllib.urlopen` 与 curl 的**隐式 env 代理**。2026-09-17 本次实测：沙箱自动注入 `HTTP_PROXY=HTTPS_PROXY=http://127.0.0.1:63370`，该 egress 代理对 `api.github.com` 返回 **`Tunnel connection failed: 502 Bad Gateway`** —— 连 `--list-orphans` 这种**纯只读**动作都直接抛 Traceback 崩溃（`api()` 的重试标记里没有"隧道/网关"类，判定为不可重试即 `raise`）；而**同一台机器直连正常**（前一日发布即直连成功）。属"环境变了但工具假设没变" | ① 传输顺序改为**直连优先**：`_api_urllib` 改用显式 `ProxyHandler({})`（禁用 env 代理），env 代理降为**兜底候选**，`_api_curl` 补 `--noproxy '*'`；新增 `--proxy URL` 供确实需要代理的场景强制指定（置 `_FORCE_PROXY`，此时不再试直连）；② `_RETRY_MARKS` 补入 `tunnel connection failed` / `bad gateway` / `proxy` / `502` / `503` / `407`；③ `remote_manifest()` 与 `create_repo()` 调用点加 `try/except`，网络不可达时**给干净提示**（含"原样重跑通常即可 / 可加 --proxy"）而**不是抛 traceback** |

**教训（2026-09-17）**：本期两处**均属"同一段文字内部自相矛盾"**，与此前的"跨模块口径互斥"（#34）是同一类病 ——
**硬编码的尾句 / 收尾句最容易逃过 review**：它不承载任何数据，读起来永远像"通顺的废话"，
但当它与**同函数上文的数据结论**（head/body）并存、或被多个方向分支共用时，就会在最强的形态下变成反向结论。
→ **推论：凡被多分支共用的 tail / 结论句，必须至少按「主方向」维度条件化；方向性否定词
（"缺乏动能 / 回落风险大于上行空间"）不得出现在上涨分支里。**
→ #39 与 #24 / #26 同族第四例（**位置 / 方向动词必须接实际点位判定**）："未再跌破开盘低点"里
每个词（未、跌破、开盘低点）都各需要一次数值校验。
→ 回归盲区复核：`index_split_hold`（retrace 0.01pct）**早已能触发 #39 的输出路径**，但因未对该句设断言而长期绿灯
——再次印证 v4.7 教训②「夹具要覆盖形态，**更要对每个模块的关键结论句设断言**」。
→ **#40 是另一类盲区：元数据没人看**。`--real` 夹具的 `meta.report_date` 错了 3 个版本、3 份夹具，
渲染输出完全不受影响（渲染器走 `--date`），回归也全绿——**"看不见"的字段恰恰是审计信任链的底座**。
教训：**凡"读一个会被就地修改的对象"，必须在修改之前读**；`meta` / 配置类字段最容易踩这个坑，
且症状（日期错）与代码逻辑（脱敏、schema 校正）完全无关，跨版本难以察觉。
→ **#41 / #42 暴露的是"工具链自身的失败语义"**：前者把"网络抖动"升级成"整次发布中断"，
后者让"部分失败"伪装成"全部成功"。二者叠加意味着 **一条命令跑完没报错 ≠ 内容真的上去了**。
凡有对外副作用的脚本：① 网络类错误必须**重试/降级**而非冒泡；② **退出码必须承载"是否全部成功"**。

→ **#43 是"本地状态即真理"这个隐含假设的反例**：`collect_files(SKILL_DIR)` 给出的是**本地上传清单**，
而"仓库当前内容"是**另一份状态**，二者只能靠比对发现差异。工具只有 PUT 时，**删除是单向的**：
本地删掉的文件在远端永久存活，且**任何本地检查都照不出来**（本地零残留、复扫也只看得到"多了一个"）。
教训：**凡"本地清单 → 远端"的单向同步工具，必须补一个反向核对（remote × local 的差集）**，
并把它做成默认可见的报告；破坏性动作（删除）则必须显式开关 + 白名单豁免 + 取不到远端状态即失败。
→ 这条与 #40「元数据没人看」是同一族：**缺陷不一定在输出里**——#40 错在看不见的字段，
#43 错在看不见的**远端状态**。审计信任链的底座恰恰由这些"没人看的地方"构成。

→ **#44 是"环境变了但工具假设没变"**：`urllib` / `curl` 都会**默默读取 env 代理**，
昨天没有代理时直连正常、今天沙箱注入代理后立刻 502，而工具对此毫无察觉（连只读命令都崩）。
教训：**凡是本地已有成功记录的对外动作，也要把"环境假设"写进工具**（这里是"直连优先、代理兜底"），
而不是依赖"上次能跑通"。且**只读路径同样需要容错**——`--list-orphans` 崩在 traceback 上，
会让使用者以为"这个功能本来就不work"，从而再也不用它。

**第六批（2026-09-10 实测发现，缺陷 #24：走势叙事"翻红"硬编码）：**

| # | 症状 | 修正 |
|---|---|---|
| 24 | 模块 02 早盘段（`hi_first=False` 的"低开→探底→回升→冲高→回落"分支）**写死"随后部分板块转强，跌幅收敛并翻红"**——当日上证盘中最高仅 -0.06%（10:39 摸高 3949.25，昨收 3951.51），**全程未转正**，与"翻红"直接矛盾 | 按日内最高点 `hi` 分三档：`hi>=0` → "跌幅收敛并翻红"；`-0.2<hi<0` → "跌幅收敛、盘中一度逼近平盘"；`hi<=-0.2` → "跌幅有所收敛，但盘中始终未能翻红"。变量 `_rec2` 就地内联于该 else 分支 |

**教训（2026-09-10）**：#24 与 #15（"盘中冲高 -0.10%"）同族——**凡在文案里断言"翻红/转涨/收复"的方向词，必须接实际点位判定，不能按分支语义想当然**。该缺陷能逃过七态回归，是因为既有夹具的 `hi_first=False` 形态里指数最高点均为正；当日"低开探底回升但最高点仍为负"是第 8 种形态。
→ 补**第 8 态夹具 `low_open_recover_merged.json`**（2026-09-10 真实采集数据脱敏：剔除 6 只真实持仓，保留 7 大指数 + 板块/广度/涨跌停/资讯；形态=低开→探底→回升→冲高→回落、上证最高 -0.06% 全程未翻红 + 广度 20.1% 普跌 + 量能 flat 111%），负断言 `{"跌幅收敛并翻红", "并已收复昨收、由跌转涨"}` 必不出现。tests 升级**八态**。

**第九批（2026-09-15 实测发现，第 11 形态「指数涨跌互现 × 个股普跌」批次 4 处：缺陷 #34/#35/#36/#37）：**

> 证据方法（本期首次系统化）：把**全部 10 份夹具 + 当日真实数据**共 11 种形态各渲一版，
> 脚本化提取 M01「结构特征」句与 M06「①市场宽度」句做横向比对。
> **硬编码的影响面必须这样量化**——单看当天数据只能看到 1 种形态，无法判断优先级。
> 关键前提：夹具 JSON 里 `snapshot[code]["pct"]` 与渲染器真正使用的 `am_pct`
> （= 分时末点价 ÷ prev_close − 1）**并不一致**，比对时必须按 `am_pct` 复算，
> 否则会得出错误的形态描述（`broad_rise` 曾被误记为"4 跌 3 涨"，实为 **4 涨 2 跌 1 平**）。

| # | 症状 | 修正 |
|---|---|---|
| 34 | **M06 的「指数 × 个股方向」判定只覆盖「指数内部分化 × 个股普涨」**（且带 `up_ratio >= 55` 条件），其余形态一律落兜底句"**指数与个股方向大体一致**"。11 形态实测 **6/11 落该兜底句**，其中 2 种明确错误：① `weight_drag`（7 指数全跌 -0.60% × 66% 个股普涨）**方向实际相反**，M01 同页写"个股普涨而指数承压、权重股拖累"，M06 却说"大体一致"；② 2026-09-15 当日（涨跌互现 × 27.9% 普跌）M01 已写"内部分化 × 个股普跌" | 改为「**指数方向（普涨/普跌/分化/全平）× 广度档（强≥55 / 中 45–55 / 弱<45）**」二维判定，共 12 象限；删除兜底句"指数与个股方向大体一致"（该串已从模板彻底移除），**无方向可言时不给方向断言** |
| 35 | 模块 07 持仓卡的板块句尾为固定串「**与个股同向/背离需结合比较。**」——**不引用任何数据**，同一日多只持仓逐字重复（当日 示例个股A +4.66%/钨 +4.75%、示例个股B +1.69%/半导体 +1.56% 两条完全相同），读起来是半句未完成的话 | 按「板块涨幅 − 个股涨幅」差值三态给出实际比较：\|差值\|<1.0 → "板块与个股涨幅基本同步"；个股领先/落后 → "个股涨幅领先/落后板块 X.XX 个百分点" |
| 36 | **M01 的 `breadth_regime == weak` 分支无条件写「指数分化 + 多数个股下跌」**，不看指数实际方向。实测 `low_open_recover`、`open_high_selloff`（均为 **7 大指数 0 涨 7 跌**）与 `expanded_bear`（1 涨 6 跌）**全部输出"指数分化"**——7 个指数全线收跌根本不是"分化" | `weak` / `neutral` 分支的"指数分化"表述按 `idx_dir()` 的 `is_split` 条件化：分化 → "指数内部分化 × 个股普跌"；非分化 → "指数与个股同步承压"。**并保留原"上午上涨主要由少数权重与主线拉动"尾句**，使既有断言继续有效 |
| 37 | **M01 开盘档位自相矛盾**：`n_up >= 5` 即判"全线高开"，与紧随其后的统计句冲突——`bull_resonance` 实测输出「**全线高开**。7 大指数中 **2 只低开**、5 只高开」 | 「全线」需对侧计数为 0：`n_up>=5 and n_down==0` → 全线高开；`n_down>=5 and n_up==0` → 全线低开；否则退到"多数高开/多数低开"。实测 bull_resonance 已改为"多数高开" |
| — | **附带修复（同源）**：M01 / M04 / M06 各自内联重复计算"7 大指数里几只涨几只跌"，阈值与用途不一致 → 同一份报告可并存三套互斥口径 | 抽出**单一事实源 `idx_dir()`**（模块级，死区内 ±0.05%），返回 `(n_up, n_down, n_flat, is_split, trend)`，三处共用；`trend ∈ {up, down, split, flat}` |
| — | **附带修复（夹具质量）**：`run_three_state.py` 中 `broad_rise` 的形态注释按 `snapshot.pct` 写成"4 跌 3 涨"，与 `am_pct` 实况（4 涨 2 跌 1 平）不符；夹具 `snapshot.pct` 与 `minutes` 口径长期不一致且无人察觉 | 修正注释并在 harness 顶部加「夹具口径提示」，明确**断言与形态描述一律以 `am_pct` 为准**；诊断复算式写在 docstring 里 |

**教训（2026-09-15）**：本期是**第四条同族铁律**——**凡"方向/关系"断言，必须先确定「用哪个指数、哪个口径、几只算多数」，
再让所有模块引用同一份事实**。前三条分别是 #15/#24/#26（方向动词要校验是否真的动过）、
#29（最高级要跨样本排序）、#34 本批（关系断言要有二维表）。
另：**夹具的 `snapshot` 与 `minutes` 两套口径可以长期不一致而不被发现**——
因为回归只断言"某句话在不在"，不断言"数值口径是否自洽"；凡是据夹具数值下判断的地方都要复算一遍。

### 回归测试（改完脚本必做）
改动 `generate_midday_review.py` 后，用**不同行情形态**渲染确认分支都触发正确、无未替换占位。
**首选仓库自带的回归套件**（不依赖本机历史数据，任何机器可跑）：
```bash
cd <skill>/tests && python run_tests.py
# 三个套件：① 采集器落盘契约 ② 发布工具孤儿检测 ③ 十三态渲染（含负断言）；全绿退出 0
```
十三份夹具覆盖 SKILL 规定的十三种形态：普涨（82%）/ 分化（46%）/ 普跌（18%）/
**权重拖累**（`weight_drag_merged.json`：7 大指数翻跌 -0.6% + 66% 个股上涨，
专测「指数方向 × 上涨占比」二维判定，断言"个股普涨而指数承压 / 权重拖累型分化 /
方向相反、权重股拖累特征明显"必现，并负断言"指数与个股方向大体一致"——v4.7 修复 #34 前，
该形态 M06 会在这句兜底文案上给出与同页 M01 相反的结论）/
**指数内部方向分化×普涨**（`index_split_hold_merged.json`：2026-09-14 真实采集数据脱敏——剔除 6 只真实持仓，
仅保留 7 大指数 + 板块/广度/涨跌停/资讯；形态 = 7 大指数 3 涨 4 跌「内部方向分化」
（上证 +0.16 / 中证1000 +0.68 涨，深成 -0.25 / 创业板 -0.47 / 科创50 -0.94 跌）+ 个股 64.4% 普涨
+ 上证开盘 3867.02 即全天最低、单边上行、**retrace 仅 0.01pct** + 量能 flat 107%，
负断言"指数与多数个股同向走强 / 指数与个股方向大体一致 / 尾盘再度回落 / 是全场日内动能最强的指数 /
窄幅整理收官"必不出现——专测缺陷 #28/#29/#30 的三个新分支）/
**指数涨跌互现×个股普跌**（`index_split_bear_merged.json`：2026-09-15 真实采集数据脱敏——剔除 6 只真实持仓，
保留 7 大指数 + 板块/广度/涨跌停/资讯；形态 = 第 11 形态「指数涨跌互现（**3 涨 2 跌 2 平**）
× 个股普跌（27.9%，1547涨/3916跌）」+ 科创50 开盘 1524.27 即全天最低、单边上行 +1.79%
（自低点修复 2.05pct 为全场最大）+ 量能缩量 98%，断言"指数内部分化 × 个股普跌 /
指数内部方向分化（3 涨 2 跌）、个股层面偏普跌"必现，负断言
"指数与个股方向大体一致 / 与个股同向/背离需结合比较 / 指数与多数个股同向走强 / 指数与个股严重背离"
必不出现——专测缺陷 #34/#35/#36/#37；**该夹具已剔除全部持仓，M07 无持仓卡，
故 #35 的正向验证须在带 `holdings.json` 的真实渲染中完成，夹具内只做负断言**）/
**科技成长普涨共振**（`broad_tech_surge_merged.json`：2026-09-16 真实采集数据脱敏——剔除全部 6 只真实持仓，
保留 7 大指数 + 板块/广度/涨跌停/资讯；形态 = 第 12 形态「**7 大指数全红**」（科创50 +4.50% 领涨、
创业板指 +2.35%、中证1000 +2.14%、上证 +0.57%）+ 个股 **76.2% 普涨**（4228涨/1150跌）
+ 上证开盘 3861.75 → 10:01 探底 3843.28 → 单边上行收 3886.48、**自高点仅回落 0.01pct**
+ 量能 flat + 涨停 64 家，断言"量价齐升 / 不过量能未同步放大，下午需以补量确认上行 /
开盘后于 10:01 下探 3843.28 形成上午低点 / 高位持稳收官"必现，
负断言"指数缺乏向上突破的动能 / 高位品种回落风险大于上行空间 / 全天未再跌破开盘低点 /
市场宽度偏弱 / 指数红、账户绿"必不出现——专测缺陷 #38/#39；**该夹具已剔除全部持仓，M07 无持仓卡**）/
**广度中性下沿×低开冲高回落**（`neutral_edge_fade_merged.json`：2026-09-17 真实采集数据脱敏——剔除全部 6 只真实持仓，
保留 7 大指数 + 板块/广度/涨跌停/资讯；形态 = 第 13 形态「**7 大指数全线低开**、盘中翻红后回落收跌」
（上证 -0.36%、上证50 -0.65% 领跌、创业板指 -0.12% 相对抗跌）+ 个股 **40.7%**（2260涨/3102跌，
落 `neutral` 40–55% 的**偏弱下沿**）+ 上证低开 -0.38%、低点 3868.10（-0.60%）**迟至 10:14** 才出现
（09:34 反先创 +0.18% 高点）+ 涨停 33 家，断言"上午低点迟至 10:14 才出现 / 并非开盘时段形成 /
下跌家数多于上涨 / 结构偏弱 / 指数普遍收跌 / 指数与个股同步走弱，缺少逆势赚钱效应"必现，
负断言"开盘后短暂下探至上午最低 / 开盘后短暂冲高至上午最高 /
行情结构偏均衡，主线与补涨并存 / 指数与个股方向一致、涨跌大致相当"必不出现——专测缺陷 #45/#46；
**该条同时是跨模块口径一致性的回归锁：M01 与 M06 的断言必须同页共存而不互斥**；
该夹具已剔除全部持仓，M07 无持仓卡）/
**普涨共振**（`bull_resonance_merged.json`：weight_drag 派生、指数翻红 +0.4% + 66% 上涨，
负断言"市场宽度偏弱 / 指数红、账户绿 / 权重拖累 / 个股普涨而指数承压 / **全线高开**"
必不出现——末项为 v4.7 #37：该夹具开盘统计为"2 只低开、5 只高开"，不得输出"全线高开"）/
**指数平×普涨**（`index_flat_bull_merged.json`：weight_drag 派生、`flip_index_down(per_code=…)`
把创业板指放 -0.15%（触发 divergence 平收分支）、其余指数 ±0.2% 平盘 + 66% 上涨，
负断言"个股弱、量能平 / 观望形态"必不出现——专测量价五分支的平收×普涨象限）/
**放量普跌**（`expanded_bear_merged.json`：2026-09-09 真实采集数据脱敏——仅保留 7 大指数、
volume_regime=expanded（推算全日 117%）+ 广度 33% + 创业板 -0.34%，
负断言"普跌缩量 / 量能亦未放大 / 量能仍不能放出 / 观望形态"必不出现——专测缺陷 #23 的
expanded 档分支；此前六态夹具全部落在 flat 档 101%，故 #23 在六态回归全绿下仍复现）/
**低开探底回升未翻红**（`low_open_recover_merged.json`：2026-09-10 真实采集数据脱敏——仅保留
7 大指数 + 板块/广度/涨跌停/资讯，形态=低开→探底→回升→冲高→回落、上证最高 -0.06% 全程未翻红
+ 广度 20.1% 普跌 + 量能 flat 111%，负断言"跌幅收敛并翻红 / 并已收复昨收、由跌转涨"必不出现
——专测缺陷 #24 的 else 分支；既有 `hi_first=False` 夹具的指数最高点均为正，故 #24 在七态全绿下仍复现）/
**开盘即高点单边下行**（`open_high_selloff_merged.json`：2026-09-11 真实采集数据脱敏——仅保留
7 大指数 + 板块/广度/涨跌停/资讯，形态=全线低开、**上证开盘 -0.60% 即全天最高**、此后单边下行
（低点 10:44 -2.08%、收 -1.82%）+ 广度 6.3% 极端普跌（350涨/5180跌）+ 量能 expanded 档 136%，
负断言"带动指数快速拉升 / 冲高后震荡回落 / 与指数冲高回落同时出现 / 指数自高点回落，显示高位分歧"
必不出现——专测缺陷 #26 的"最高价=开盘价却写冲高"分支；此前 `hi_first=True` 夹具的最高点均高于开盘价，
故 #26 在八态全绿下仍复现。注意该日创业板指自开盘上行 0.46pct，其"冲高回落"属真实，故负断言只封上证口径文案）。
新增夹具方法（v4.7 起已固化为命令，不必再手工拼脚本）：把当日 `midday_merged_{date}.json`
**脱敏后直接落 `fixtures/`** —— 真实形态（放量档、未翻红档、分化×普跌档等）用真实采集数据
比从历史基底派生更保真。`build_fixtures.py` 现同时支持两种模式：
```bash
# ① 派生模式（无参数）：以历史基底合成 普涨/分化/普跌/权重拖累，再由权重拖累派生出
#    普涨共振、指数平×普涨（唯一变量=指数方向，用于隔离单个分支）
MIDDAY_BASE=<某次真实采集的 midday_merged_*.json> python build_fixtures.py

# ② 真实脱敏模式（v4.7 新增，真实形态首选）：
MIDDAY_PRIVACY="sh123456,sz128789" python build_fixtures.py \
  --real <midday_merged_YYYYMMDD.json> --regime <形态名> \
  --out <形态名>_merged.json --note "形态一句话说明，源=YYYY-MM-DD 真实采集脱敏"
#    脚本会自动剔除隐私代码、校正 v2 schema、写 meta，并在落盘后做**脱敏自检**
#    （检出残留即以退出码 3 失败，避免把持仓代码提交进公开仓库）
#    未取到脱敏名单时**拒绝出件**（退出码 4），除非显式 --allow-no-privacy
```
⚠️ **隐私名单只从外部注入，仓库内不保留任何真实持仓**（v4.7 起）：
`PRIVACY = _privacy_codes()` 按序取 ① `MIDDAY_PRIVACY` 环境变量（逗号分隔，支持
`sz123456` / `123456` 两种写法）→ ② skill 目录 / 其上一级 / 当前工作目录的 `holdings.json`
（与渲染器共用同一份外部配置）。**历史做法是在源码里硬编码 `PRIVACY` 常量**，两个致命问题：
① 常量本身就把真实持仓写进了待发布的公开文件；② 持仓会变动，硬编码必然滞后，
实际早已失效（旧列表不含当前持仓，等于没有保护）。

⚠️ **脱敏覆盖面（缺一即可能泄露，2026-09-15 真实事故）**：`snapshot` / `minutes` / `kline` /
`fundflow` / **`sector_industry`+`sector_concept` 的 `lead` 与 `lead_code`** /
**`zt_pool`+`dt_pool` 的个股行**。旧版只清前四处，**漏了板块的龙头字段** ——
一份真实夹具里"钨"板块的 `lead` / `lead_code` 正是持仓个股，且该字段位于 **JSON 第 1 行**，
在 400KB 单行文件里肉眼 review 完全不可见。**这类字段靠人工检查是查不出来的，必须有工具闸门。**


有本机历史 merged 数据时，可额外加跑真实日期回归：
```bash
for d in 20260821 20260824 20260825; do
  python generate_midday_review.py --date ${d:0:4}-${d:4:2}-${d:6:2} \
    --merged midday_merged_$d.json --breadth breadth_$d.json --out .tmpdata/regress_$d.html
  grep -o "{[A-Z_][A-Za-z_0-9]*}" .tmpdata/regress_$d.html | sort -u   # 应为空
done
```
**务必覆盖至少十三种形态：普涨、普跌、指数跌+广度强（权重拖累）、指数红+普涨（共振）、
指数平+普涨、分化震荡、放量普跌、低开探底回升但未翻红、开盘即高点单边下行（最高价=开盘价）、
指数内部方向分化（7 大指数 3 涨 4 跌 × 个股普涨 + retrace≈0 持稳）、
指数涨跌互现 × 个股普跌（3 涨 2 跌 2 平 × 27.9% 普跌，前者的镜像）、
科技成长普涨共振（7 大指数全红 × 76.2% 普涨 + retrace 0.01pct 收在高位）、
广度中性下沿 × 低开冲高回落（7 大指数全跌 × 40.7% 落 neutral 偏弱下沿 + 上午低点迟至 10:14）。**
只测当天数据会漏掉一多半分支（2026-09-02：当天修复 10 处后，仅测当天全绿，
一跑 8/25 历史数据立刻又暴露 5 处"指数跌但个股普涨"分支的矛盾；
2026-09-04 同理：仅测当天也无法暴露 #16-20，须靠 bull_resonance 夹具负断言；
2026-09-09 同理：#23 在六态全绿下复现，因六态全落 flat 量能档；
2026-09-10 同理：#24 在七态全绿下复现，因既有 `hi_first=False` 夹具的指数最高点均为正；
**2026-09-15 同理：#34/#36 在十态全绿下仍有 6/11 形态落错分支，因既有夹具从未同时具备
「指数涨跌互现 + 个股普跌」——#34 甚至在 weight_drag（已有夹具！）上就已复现，
只是该夹具没有对 M06 句子设过断言。
2026-09-17 同理（第二批）：#45 在十二态全绿下复现——既有夹具的**低点全部落在开盘段内**，
"开盘后短暂下探"从未被证伪；#46 更隐蔽，`differentiation`（46%）**一直在跑 neutral 分支**，
但它的断言恰恰是那句错话，等于给错误上了锁。**）
**2026-09-17 同理：#38/#39 在十一态全绿下仍复现——#38 的分支 bull_resonance 早已覆盖、
#39 的输出路径 index_split_hold 早已走到，两者都因「没给这两句设断言」而长期绿灯。**）
**→ 推论：夹具不仅要覆盖形态，还要对「每个模块的关键结论句」都设断言**；
只断言 M01 而不管 M06，等于把跨模块一致性交给了运气。

## 分发到 GitHub（可选，v4.7 加脱敏闸门，v4.8 加网络重试与失败退出码，v4.9 加远端孤儿核对与代理治理）

`tools/deploy_to_github.py` 走 GitHub REST API（`api.github.com`）建仓并上传，
**不用 `git push`**——规避 `github.com:443` 上行 TLS 不稳（本机间歇通、备用机全拦）。
token 优先 `--token`，省略时自动从 `git credential fill` 取（不在命令行暴露）。

```bash
# ① 先只评审（推荐：确认闸门与清单，零外部动作）
python tools/deploy_to_github.py --owner <user> --repo midday-a-share-review --public --audit-only

# ② 顺便做远端孤儿体检（只读，需 token）
python tools/deploy_to_github.py --owner <user> --repo midday-a-share-review --public --list-orphans

# ③ 通过后再发布；若体检报了孤儿，加 --prune-orphans 一并清理
python tools/deploy_to_github.py --owner <user> --repo midday-a-share-review --public --template \
    --prune-orphans
```

**远端孤儿核对（v4.9，缺陷 #43 修复）**：本工具**只做 PUT，没有 DELETE 语义**，
所以"本地删了"从不等于"远端删了"——`tests/breadth_20260824.json` 就是 2026-09-01 发布留下的
远端尸体（本地 09-15 已删、`SKIP_GLOBS` 也已挡住，但远端副本一直活到 09-17 才被发现）。
- `--list-orphans`：列出**远端有、本地上传清单没有**的文件，**零写入**（可与 `--audit-only` 同用）。
- `--prune-orphans`：**显式**删除这些孤儿（逐文件 DELETE，幂等，中途失败打全清单并非零退出）。
- `--keep-remote 'LICENSE,assets/*'`：追加豁免通配；内置 `KEEP_REMOTE` 默认豁免
  `LICENSE` / `NOTICE` / `.github/**` / `CHANGELOG.md`，避免误删**仅在网页端维护**的文件。
- **三重安全**：孤儿处理只在上传**全部成功之后**跑（半态下会把刚失败的文件误判为孤儿）；
  取不到远端清单时**直接判失败**而非当作"远端为空"；删除必须显式开关，默认永不删。

**网络与代理（v4.8 缺陷 #41/#42，v4.9 缺陷 #44）**：
- `api.github.com` 在本机（家宽 / 沙箱）是**间歇可达**的，因此 `api()` 自带
  **指数退避重试（4 轮）**；**只重试网络类异常**，已拿到 HTTP 状态码（含 4xx/5xx）
  一律直接返回，不会掩盖权限等问题。
- **传输顺序：直连优先 → env/`--proxy` 代理兜底 → curl 后端**（curl 走独立 TCP 栈，
  抖动时往往能通）。**沙箱可能注入 `HTTP_PROXY` 指向本地 egress 代理，实测该代理对
  GitHub 返回 `502 Bad Gateway`，故默认显式禁用 env 代理**（`ProxyHandler({})` +
  curl `--noproxy '*'`）；确实需要代理时用 `--proxy <url>` 强制指定。
- 逐文件上传循环会收集失败清单：**任一文件失败即 `sys.exit(1)`**，全部成功才 exit 0。
  → **退出码可以信任**：`0` = 全部上传成功；`1` = 有文件失败（打印明细）。
  失败多为网络抖动，**直接重跑同一条命令即可**（逐文件 PUT 是幂等的）。
- 发布后**务必做一次远端复扫**（本地审计只证明"源文件干净"，不证明"传上去的是干净的"）：
  用 `GET /repos/{owner}/{repo}/git/trees/main?recursive=1` 列出全部 blob，
  逐个 `GET /git/blobs/{sha}` 拉回文本再跑一遍 `leak_scan()`。**v4.8 首次发布的实测结果：
  27 个 blob / 4.89 MB 全部无残留。**

**脱敏闸门（`audit_privacy()`）在 `create_repo` 之前执行**：扫描全部待上传的文本文件，
命中真实持仓代码/名称即**中止发布**（退出码 5）。未取到脱敏名单时同样中止（退出码 6，
除非 `--allow-no-privacy`）。跳过闸门需显式 `--allow-privacy-leak`（极不推荐）。

> **为什么必须有工具闸门（2026-09-15 事故）**：本 Skill 一度把真实持仓写进了
> `SKILL.md` 的证据记录、渲染器注释、`tests/README.md` 与一份夹具 JSON，
> 一路走到"准备发布公开仓库"才被人工发现——而其中最关键的一处
> （夹具 `sector_industry[].lead`）位于 400KB 单行 JSON 的第 1 行，**人工 review 不可能看见**。
> 发布工具此前**没有任何隐私校验**，全靠自觉。**靠自觉的安全措施不叫安全措施。**
> 闸门复用了与 `build_fixtures.py` 同一套名单解析（外部注入）与同一套匹配规则
> （6 位数字按「数字边界」匹配，避免把 `-1234568816.0` 这类长数字尾段误判为代码）。

## 变更记录
- **v4.10（2026-09-17，同日第三批）**：修复第 13 形态「广度中性下沿 × 低开冲高回落」批次 2 处
  （缺陷 #45/#46）+ tests 升级**十三态**（新增 `neutral_edge_fade` 真实脱敏夹具）。
  1. **#45（时段动词未接实际时刻，#39 同族第二例）**：模块 02 **开盘段**对低开日无条件写
     "开盘后短暂下探至上午最低 X"，未校验低点出现时刻。2026-09-17 上证低开 -0.38%，
     上午低点 3868.10（-0.60%）**迟至 10:14** 才出现（09:34 反先创 +0.18% 高点）——
     既把盘中极值错记成开盘时段，又与同模块下文的"早盘 09:34 冲高 / 盘中 10:14 回落"矛盾。
     修：新增 `OPEN_END = "0940"`（对齐开盘段标题），按极值实际出现时刻判定；
     **高开侧对称分支一并修**（"开盘后短暂冲高至上午最高"在最高点落于盘中时同样不成立）。
     这条同时说明 v4.8 只修尾盘句是不够的——**同族句必须横向 grep 一次全修**。
  2. **#46（分档粒度与全库不一致，#34 同族第二例）**：模块 01 的 `neutral` 分支（40–55%）
     **一档到底**，无条件写"涨跌大致相当 / 行情结构偏均衡 / 主线与补涨并存"，
     而全库其余 **9 处**广度判定一律在 **45** 分档。2026-09-17 up_ratio = 40.7% 已贴弱档下沿，
     该句与同页 M06"指数与个股同步走弱，缺少逆势赚钱效应"、M02"主线与个股表现分化"互斥。
     修：neutral 内以 45 再分两档 + 与 `idx_dir()` 的 `_trend` 四态组合（复用单一事实源）。
  3. **tests 升级十三态**：新增 `neutral_edge_fade` 夹具（2026-09-17 真实采集数据脱敏——
     剔除全部 6 只真实持仓，保留 7 大指数 + 板块/广度/涨跌停/资讯），正断言 6 条 / 负断言 4 条；
     该条同时是**跨模块口径一致性**的回归锁（M01 与 M06 必须同页共存而不互斥）。
  4. **顺带增强（非缺陷，属"效果一致性"）**：`--mcp-note` 省略时的**默认页脚**原先只声明"用了什么源"
     （公开接口 / 未调用任何 MCP），**未声明"没做什么校验"**。实测同一份数据、同一渲染器，
     **带与不带 `--mcp-note`** 的页脚差异明显——前者含交叉校验结论与精度下降项，后者二者皆无，
     即"报告可信度说明取决于调用方有没有记得写"。现默认页脚补齐
     **"本期未做连接器侧逐项交叉校验 + 精度下降项（资金流仅当日口径 / 板块未做第二源比对）"**，
     并说明可用 `--mcp-note` 覆盖为逐项交叉校验结论。精度下降项应**默认可见**——
     它是读报告的人判断可信区间的依据，不该靠人肉补。
- **v4.9（2026-09-17，同日第二批）**：发布工具链 2 处（缺陷 #43/#44）+ 新增单测 `test_deploy_orphans.py`
  （tests 由 2 个套件增至 3 个）。
  1. **#43（远端孤儿）**：工具只有 PUT 没有 DELETE，"本地删了"从不等于"远端删了"——
     `tests/breadth_20260824.json` 在远端存活到 09-17 才被发现。
     新增 `remote_manifest()` / `find_orphans()` / `delete_remote_file()` / `list_or_prune()`
     + `--list-orphans` / `--prune-orphans` / `--keep-remote`，并加**三重安全**
     （仅上传全成功后执行 / KEEP_REMOTE 白名单 / 取不到远端清单即失败）。
  2. **#44（隐式 env 代理）**：沙箱注入 `HTTP_PROXY=127.0.0.1:63370` 后，
     `urllib` 与 curl 都会静默走它，而该代理对 GitHub 返回 `502 Bad Gateway`，
     连只读的 `--list-orphans` 都崩在 traceback 上。改为**直连优先**（显式 `ProxyHandler({})`
     + curl `--noproxy '*'`），env 代理降为兜底，新增 `--proxy` 强制指定；
     `_RETRY_MARKS` 补入隧道/网关类标记；`remote_manifest()` / `create_repo()` 加干净的错误提示。
  3. **本次实际清理**：`--prune-orphans` 删除了远端唯一孤儿 `tests/breadth_20260824.json`
     （162 B，`--selftest` 合成产物，零引用）；清理后远端孤儿数归零。
- **v4.8（2026-09-17）**：修复第 12 形态「科技成长主导的普涨共振」批次 3 处（缺陷 #38/#39/#40）
  + tests 升级**十二态**（新增 `broad_tech_surge` 真实脱敏夹具）。
  1. **#38（同一段文字内部自相矛盾）**：模块 04 `build_divergence_note()` 的结论尾句（非放量档）
     无条件写死"**指数缺乏向上突破的动能**，下午若量能仍不能放出，高位品种回落风险大于上行空间"，
     而该 `tail` 被**全部 5 个方向分支共用**（含 `cyb>=0.3` 的上涨分支）。2026-09-16 实测：
     创业板指 +2.35%、科创50 +4.50%、上证自日内高点仅回落 0.01pct 收在高位，同函数 head/body
     已写"量价齐升、有 76% 个股上涨、赚钱效应较广"，尾句却仍作方向性否定。
     修：`tail` 按「指数方向（`cyb` 三态）× 量能档（`_VREG` 二态）」二维化共 6 种措辞，
     上涨档改为"不过量能未同步放大，下午需以补量确认上行——若午后成交无法跟进，则上行空间或受限"，
     **上涨档一律不得再出现方向性否定判断**；下跌档保留原措辞（该形态下成立），平收档改中性。
  2. **#39**：模块 02 尾盘句在 `retrace<=0.05` 时写死"全天未再跌破开盘低点"，
     但"上午低点高于开盘价"**并非必然成立**（2026-09-16 上午低点 3843.28 **低于**开盘 3861.75，10:01 触及）。
     修：按「最低价 vs 开盘价」两态 + 日内低点出现时间条件化
     （`low>=open` → 未跌破开盘价 / 低点在 09:35 前 → 短暂下探后回升 / 其余 → 带 HH:MM 与点位的实际表述）。
  3. **tests 升级十二态**：新增 `broad_tech_surge` 夹具（2026-09-16 真实采集数据脱敏——剔除全部 6 只真实持仓，
     7 大指数全红 + 76.2% 普涨 + retrace 0.01pct + 量能 flat + 涨停 64），
     正断言 4 条 / 负断言 5 条，专测 #38/#39。
     **该夹具的断言必须是句子级**（如"不过量能未同步放大，下午需以补量确认上行"），
     不能按关键词全域封杀 —— 同日其它方向分支仍可能出现相近措辞。
  4. **#40（夹具元数据 / 审计信任链）**：`tests/build_fixtures.py` 的 `--real` 模式因**对象别名**写出错误
     `meta` —— `coerce_base(d)` **原地**把 `d["meta"]` 重置为硬编码的 `2026-08-21 11:30:00`，
     而它收到的 `d` 就是 `real_fixture()` 里的 `base` 本身；旧代码在 `coerce_base(base)` **之后**才读
     `src_meta`，读到的正是被覆盖后的值。结果全部 `--real` 夹具的 `report_date` / `collected_at`
     长期写错（`low_open_recover` 源 09-10、`index_split_bear` 源 09-15、新 `broad_tech_surge` 源 09-16）。
     修：`src_meta` 取值前移到 `coerce_base()` 之前并加警示注释；3 份受影响夹具用修复后脚本重新生成，
     已逐字段核验**除 meta 日期外完全等价**（无其他漂移）。
     **该缺陷渲染与回归均不可见**（渲染器走 `--date`，回归只断言文案在不在）——
     属"看不见的字段"，恰是审计信任链的底座。
  5. **#41 / #42（发布工具链可靠性）**：`tools/deploy_to_github.py` 的 `api()` 原先**只在 DNS 失败时**
     才降级 curl，对超时 / 连接重置直接抛异常 —— 本机对 `api.github.com` 间歇可达，实测本次发布
     **第 1 次尝试即在中间文件上整脚本 abort**。修：加指数退避重试（4 轮）+ 连接类异常同样降级 curl，
     且**只重试网络类异常**（拿到状态码的一律直接返回，不掩盖 4xx/5xx）。同时 `main()` 原先把上传失败
     只打印一行 `[FAIL]` 却仍以退出码 0 结束（**假成功**）→ 改为收集失败列表、有失败即 `sys.exit(1)`。
     二者叠加的教训：**一条命令跑完没报错 ≠ 内容真的上去了**。
- **v4.7（2026-09-15）**：修复第 11 形态「指数涨跌互现 × 个股普跌」批次 4 处（缺陷 #34/#35/#36/#37）
  + 抽出「指数内部方向」单一事实源 + 修正降级判据（工具索引会话级固化）+ 夹具制作命令化
  + **公开泄露修复与「发布前脱敏闸门」**。
  1. **#34（核心，跨模块方向口径互斥）**：模块 06 的「指数 × 个股方向」判定**只覆盖
     「指数内部分化 × 个股普涨」**（且带 `up_ratio >= 55` 硬条件），其余形态一律落兜底句
     "**指数与个股方向大体一致**"。11 种形态实测 **6/11 落该兜底句**，其中 **weight_drag
     （已有夹具！）** 与 **2026-09-15 当日** 两例明确错误——前者 7 指数全跌而 66% 个股上涨
     （方向相反，M01 同页写"权重股拖累"），后者 M01 已写"内部分化 × 个股普跌"。
     修：改为「指数方向（普涨/普跌/分化/全平）× 广度档（强≥55 / 中 45–55 / 弱<45）」二维判定，
     彻底移除兜底句；无方向可言时不给方向断言。
  2. **#35**：模块 07 持仓卡板块句尾为固定串「**与个股同向/背离需结合比较。**」——不引用任何数据、
     同日多只持仓逐字重复。修：按「板块涨幅 − 个股涨幅」差值三态给出实际比较结论。
  3. **#36**：模块 01 的 `breadth_regime == weak` / `neutral` 分支**无条件写"指数分化"**——
     实测 `low_open_recover`、`open_high_selloff`（均为 7 大指数 **0 涨 7 跌**）与 `expanded_bear`
     （1 涨 6 跌）全部输出"指数分化"，而全线收跌根本不是分化。修：按 `idx_dir()` 的 `is_split`
     条件化（分化 → "指数内部分化 × 个股普跌"；非分化 → "指数与个股同步承压"），
     并保留原"上午上涨主要由少数权重与主线拉动"尾句使既有断言继续有效。
  4. **#37**：模块 01 开盘档位「全线」未校验对侧计数——`bull_resonance` 实测输出
     「**全线高开**。7 大指数中 **2 只低开**、5 只高开」自相矛盾。修：`n_up>=5 and n_down==0`
     才判"全线高开"（低开对称），否则退到"多数高开/多数低开"。
  5. **附带修复**：`idx_dir()`——M01 / M04 / M06 原先各自内联重复计算 7 大指数涨跌只数，
     阈值与用途不一致，导致同一份报告并存三套互斥口径；现抽出模块级单一事实源共用。
  6. **附带修复（夹具质量）**：修正 `run_three_state.py` 中 `broad_rise` 的形态注释——
     此前按夹具 `snapshot.pct` 记为"4 跌 3 涨"，按**渲染真实口径 `am_pct`** 复算实为
     **4 涨 2 跌 1 平**；并在 harness 顶部加「夹具口径提示」，明确断言与形态描述一律以 `am_pct`
     为准（夹具的 `snapshot.pct` 与 `minutes` 两套口径可以长期不一致而不被发现）。
  7. **降级判据再修正**：新增「**工具索引是会话级固化的**」实证表——连接器在会话开始后才连上时，
     本会话的工具索引不会回注（13:29 状态面板已显示 westock-mcp connected，
     但 ToolSearch 与 `DeferExecuteTool` 全名直调**双双仍失败**）。结论：同一会话内重试 3 次足够，
     **换新会话才是真正的恢复手段**。另明确「**工具在册 ≠ 接口可用**」：当日同花顺
     `special_data_limit_up_pool` / `limit_down_pool` 正常，但三个快照/历史接口全部报
     `data/data must be object` → 交叉校验须**按接口分别登记成败**，不得因"连接器 connected"
     就假定指数校验已完成。
  8. **tests 升级十一态**：新增 `index_split_bear` 夹具（2026-09-15 真实采集数据脱敏——剔除 6 只真实持仓，
     保留 7 大指数 + 板块/广度/涨跌停/资讯；形态 = 指数涨跌互现（3 涨 2 跌 2 平）× 个股普跌 27.9%
     + 科创50 开盘即全天最低单边上行 +1.79% + 量能缩量 98%，负断言 4 条）；
     并给 `weight_drag`（+1 断言 1 负断言）、`bull_resonance`（+1 断言 1 负断言）、
     `low_open_recover` / `open_high_selloff` / `expanded_bear`（各 +1 负断言）补上本期新分支的回归保护。
  9. **夹具制作命令化**：`build_fixtures.py` 新增 `--real <采集文件> --regime <名> --out <文件> --note <说明>`
     真实脱敏模式；**隐私名单改为外部注入**（`PRIVACY = _privacy_codes()`：`MIDDAY_PRIVACY` 环境变量
     → `holdings.json`），源码内不再保留任何真实持仓；名单为空时**拒绝出件**（退出码 4）。
  10. **公开泄露修复（本批次真实事故，共 5 个文件）**：`SKILL.md`（证据记录里的持仓 ETF/个股名）、
      `scripts/generate_midday_review.py`（#33/#35 修复注释）、`tests/README.md`、
      `tests/build_fixtures.py`（示例值）、`tests/fixtures/low_open_recover_merged.json`
      （**`sector_industry[].lead` / `lead_code` 未脱敏**）——全部改用中性示例
      （`sh11xxxx`「某通信主题ETF」/「示例个股A/B」）。**根因两条**：
      ① 旧脱敏只清 `snapshot`/`minutes`/`kline`/`fundflow`，**漏了 sector 的龙头字段**，
      而该字段位于 400KB 单行 JSON 的**第 1 行**，肉眼 review 不可能发现；
      ② **发布工具（`tools/deploy_to_github.py`）此前完全没有隐私校验**，全靠人工自觉。
  11. **新增发布前脱敏闸门（本次最有价值的一处）**：`deploy_to_github.py` 增加
      `privacy_tokens()` + `leak_scan()` + `audit_privacy()`，**在 `create_repo` 之前**扫描全部
      待上传文本文件，命中即**中止发布**（退出码 5；无名单则退出码 6）；新增 `--audit-only`
      （只评审零外部动作）与逃生舱 `--allow-privacy-leak`。名单解析与匹配规则和
      `build_fixtures.py` 共用同一套逻辑。同时把 `holdings.json` 与 4 个本机调试产物
      （`.runlog.txt` / `_final.txt` / `_runlog_utf8.txt` / `_run_utf8.py`）列入 `SKIP_NAMES` 与 `.gitignore`；
      **该 4 个文件连同 `tests/breadth_20260824.json`（`--selftest` 手工残留）已于 2026-09-15 一并删除**，
      清单条目保留作防御。另新增 `SKIP_GLOBS` 通配排除（`breadth_*.json` / `midday_merged_*.json`）——
      ⚠️ **但当时只删了本地**：`breadth_20260824.json` 的**远端副本一直存活到 2026-09-17**
      （09-01 那次发布留下的），直到 v4.9 补上远端孤儿核对（缺陷 #43）才被 `--prune-orphans` 清掉。
      → 这正是本 Skill 反复强调的同一类教训：**"我做了清理" ≠ "状态真的被清理了"**，
      凡是跨"本地/远端"两个状态的动作，都必须有一次**反向核对**来证明。
      散落的采集产物此前不在任何排除规则内，是个真实盲区。
  12. **附带修复（闸门误报）**：`leak_scan()` 原为朴素子串匹配，**会把 JSON 里长数字的尾段当成代码**——
      实测某持仓代码与其所在板块资金流 `netflow: -1234568816.0` 的尾段完全相同，导致脱敏**明明成功**
      却报"脱敏失败"（退出码 3）。改为 6 位纯数字按**数字边界**匹配
      （`(?<![\d.])CODE(?!\d)`）：`"lead_code": "123456"` / `sz123456` 仍命中，`-1234568816.0` 不命中。
      **安全闸门的误报比漏报更危险——它会训练使用者忽略告警。**
  - **本期教训（第四条同族铁律）**：**凡「方向/关系」断言，必须先确定「用哪个指数、哪个口径、
    几只算多数」，再让所有模块引用同一份事实**。前三条同族铁律依次是：方向动词必须校验
    "相对基准点是否真的动过"（#15/#24/#26）、最高级表述必须由跨样本排序得出（#29）、
    关系断言必须做成二维表（#34）。另有两条方法论收获：① **硬编码影响面要用多形态横向比对量化**
    （本期把 10 份夹具 + 当日数据共 11 形态各渲一版、脚本化提取 M01/M06 原句比对，
    才看清"6/11 落错分支"而非个例）；② **夹具不仅要覆盖形态，还要对每个模块的关键结论句都设断言**
    ——#34 在已有夹具 `weight_drag` 上就已复现，只因从未对 M06 句子设过断言而长期未被发现。
  - **本期教训（第 10/11 项，隐私工程）**：**「人工 review 不可见的字段」必须靠工具兜底，
    靠自觉的安全措施不叫安全措施**。判断一处脱敏是否真的到位，标准不是"我看过一遍没问题"，
    而是**"有没有一个能自动失败的程序"**：夹具侧是 `leak_scan()` 自检（退出码 3/4），
    发布侧是 `audit_privacy()` 闸门（退出码 5/6）。另：**隐私名单必须外置**——
    写进源码的名单既泄露自身、又必然滞后于持仓变动，等于双重失效。
  13. **跨 Agent 通用化（WorkBuddy / Codex / 其他，2026-09-15 追加）**：本 skill 的**内容格式本就符合
     Agent Skills 开放标准**，通用化的真正障碍不在代码，而在两处会「静默把人带错路」的文档表述：
     ① `README.md` 把 Codex 项目级安装路径写成 `.codex/skills/`，而项目级实际已收敛到**中立目录
     `.agents/skills/`**（Codex 自 cwd 向上搜索至仓库根）；用户级为 `~/.agents/skills/`
     （同时兼容 `$CODEX_HOME/skills/`，默认 `~/.codex/skills/`）。已补 Cursor / Gemini CLI /
     GitHub Copilot / OpenCode 共用同一路径，并明确 **Claude Code 是例外**（只读 `.claude/skills/`）。
     ② **未声明环境能力差异**：降级链第 1–3 级（`westock-mcp` / `tdx-connector` / `hithink-finance`）
     是 **WorkBuddy 连接器专属**，Codex 下**只有第 4 级公开 API 可用**——已明确该环境下这是
     **正常路径而非降级**（两个脚本零 MCP 依赖、开箱即用），并禁止在 `sources` 里谎称调用过 MCP。
     另补：新建 `agents/openai.yaml`（Codex 侧 `display_name` / `short_description` / `default_prompt`）；
     frontmatter 新增标准可选字段 `compatibility`，并把 **`version` 4.2 → 4.7**（此前 5 个版本未同步）。
  - **本期教训（第 13 项，跨工具移植）**：**`.agents/skills/` 是实现约定，不是规范强制**——
     规范只定义目录内部结构（`SKILL.md` + `name` / `description`），**不规定任何搜索路径**。
     所以移植性必须在「内容 / 发现 / 调用 / 运行时」**四层分别验证**，
     **通过第一层（格式合法）不代表第四层（实际可跑）可用**。
     本 skill 的可用性恰在于四层都经实测：脚本零第三方依赖、零 MCP 依赖、零外部 CDN。

- **v4.6（2026-09-14）**：修复第 10 形态「指数内部方向分化 × 个股普涨」批次 5 处（缺陷 #28/#29/#30/#32/#33）
  + 修正「ToolSearch 查不到即判未注册」的错误降级判据 + 新增数据源「行情日期一致性」校验。
  1. **#28（核心，报告自相矛盾）**：模块 01/06 的「指数 × 个股方向」只看**上证**一个指数，
     与模块 04 用**创业板**口径得出的"方向相反、权重拖累"在同一天并存 → 输出两套互斥结论。
     修：新增「指数内部分歧度」维度（7 大指数中 ≥2 只涨 且 ≥2 只跌 → 内部方向分化），
     落点 3 处（模块 01 宽度联动新增 `_split` 分支、模块 06 `_rel` 判定、模块 04 量价涨分支 `_rel2`）。
  2. **#29**：模块 01「为全场**最弱/最强**的日内动能表现」是**硬编码最高级、未做跨指数排名** → 改为按
     `am_pct-lo_pct`（修复幅度）与 `retrace`（回落幅度）跨 7 指数排序，第 1 名才用"全场最大"，
     其余写"居全场第 N（最大为 XXX 的 Y.YY 个百分点）"。
  3. **#30（用户直接指出）**：模块 02 `hi_first=False` 分支把「部分板块发力，指数反弹冲高」
     「但量能未能跟进，尾盘再度回落」**整句写死**，不引用任何数据 → **实测 6 个交易日中 4 天走本分支
     却输出逐字相同的句子**（09-08 retrace 0.10 / 09-09 0.17 / 09-10 0.29 / 09-14 **0.01** 全部写"尾盘再度回落"）。
     修：按 `hi`（是否翻正）与 `retrace` 三档条件化（≤0.15 持稳 / ≥0.6 明显回落 / 其余小幅回落）；
     尾盘句同步改为数据驱动（retrace≤0.05 → "高位持稳收官"，并删掉写死的"多空分歧显著"）。
     顺带修一个**拼接 bug**：模板 `"基本守住日内高位。"` 后接以 `。` 开头的句子 → 输出 `。。` 双句号。
  4. **#32**：操作总纲② 写死「科创50 `<昨收>`（昨收，破位则成长股离场）」，但当日科创50 上午收 1538.84
     **已低于昨收 0.94%，"破位"条件其实早已触发**，仍按"待破位"表述会误导 → 新增状态校验：
     已破位时改写为「上午低点；现价 X 已低于昨收 Y，再破上午低点则走弱信号强化」。
  5. **#33**：模块 08 风险条「净流出板块与持仓方向重叠」判定**只按 `hold_ctx.sector` 精确匹配**，
     而示例 ETF（`sh11xxxx`「某通信主题ETF」）的 sector 为 `null` → 「通信 -52.0亿」未判为重叠，报告写
     "与持仓所属方向无直接重叠"，但该 ETF 恰为当日组合最弱（-2.07%）→ 补：从持仓名称剥离
     ETF / 基金公司 / 转债 等后缀取关键词（"某通信主题ETF"→"通信"）后双向包含匹配。
  6. **数据源：新增「行情日期一致性」校验**（`collect_midday_data.py`）。腾讯分时接口在 `data` 节点下
     **自带 `date` 字段**（如 `"20260914"`），原实现只读 `data` 数组、丢弃了它 →
     `--date == 今日` 闸门只挡得住"把今天数据写成历史日期"，**挡不住反方向**：周末运行 DATE==周六
     照样通过，接口给的却是周五分时 → 报告被标成周六却用了周五数据。现写入质量闸门，
     `QUOTE_DATES` 与报告日不一致即判 **fail 并阻断出报告**。
     另修死代码：「分时末点非11:30」原判据 `ms[-1]["t"] > "1130"` 在数据已截断到 ≤1130 的前提下
     **恒不成立**，改为校验 strict-midday 模式下末点必须正好是 `1130`。
  7. **降级判据修正**：新增「⚠️⚠️ ToolSearch 查不到 ≠ 工具未注册」节（见上），附 09-14 三时刻实证表。
  8. **tests 升级十态**：新增 `index_split_hold` 夹具（2026-09-14 真实采集数据脱敏——剔除 6 只真实持仓，
     仅保留 7 大指数 + 板块/广度/涨跌停/资讯；形态 = 7 大指数 3 涨 4 跌「内部方向分化」+ 个股 64.4% 普涨
     + 上证开盘即全天最低、单边上行、**retrace 仅 0.01pct** + 量能 flat 107%，负断言 5 条）。
     **同时修正既有 `broad_rise` 夹具的错误断言**：该夹具 7 大指数实为 4 跌 3 涨 × 82% 普涨，
     属「指数内部分化 + 个股普涨」，旧断言"行情具备一定广度支撑"（即"指数与多数个股同向走强"）
     **本身就是错的那句，被固化了 9 个版本** → 改为断言正确的分化表述，并把旧文案列入负断言。
  - **本期教训（第三条同族铁律）**：① **凡"最高级"表述（最强/最弱/最大/唯一）必须由跨样本排序得出**，
    不能按分支语义想当然（与 #15/#24/#26 的"方向动词"同族）；② **硬编码的影响面要用多日实测量化**——
    本期用 6 个历史交易日跑同一分支，才看清"4/6 天输出同一句"而非偶发；
    ③ **夹具的"期望文案"也可能是错的**——当某个夹具长期绿灯却编码了错误行为时，修脚本必须同步修断言。

- **v4.5（2026-09-11）**：修复第 9 形态「最高价 = 开盘价、全天未上行」批次 3 处（缺陷 #25/#26/#27）。#26 是 #15/#24 同族第三次复发——**补第二个判据 `_lift = hi_pct - open_pct`，`_lift >= 0.15` 才允许"冲高/拉升/高位分歧"**，落点 5 处（模块 02 elif 分支、`index_pattern` 新增 `open_high_fall`、模块 04 `_PAT_CN`、模块 07 `shape`、模块 08 风险条+机会条）；#25 持仓标签改为**组合内相对排名**（"最弱/最强"各仅一次）并给 `build_hold_note` 加 `label` 参数；#27 风险条板块选择由"涨幅榜前两个净流出"改为"净流出额前 2"并标注是否与持仓方向重叠。tests 升级**九态**——新增 `open_high_selloff` 夹具（2026-09-11 真实采集数据脱敏：上证开 -0.60% 即全天最高、单边下行收 -1.82% + 广度 6.3% 极端普跌 + 量能 expanded 136%，负断言 4 条）。教训：**凡方向动词（冲高/拉升/翻红/收复/高位分歧）必须校验"相对基准点是否真的动过"**——`hi_first`/`retrace`/`bounce` 只描述时序与幅度差，不含该信息；且**夹具负断言须精确到句子、不能按关键词全域封杀**（同日创业板指自开盘上行 0.46pct，"冲高回落"对它是真实表述）。
- **v4.4（2026-09-10）**：修复缺陷 #24——模块 02 走势叙事 `hi_first=False` 分支（低开→探底→回升→冲高→回落）写死"随后部分板块转强，**跌幅收敛并翻红**"，当日上证盘中最高仅 -0.06%（10:39 摸高 3949.25 / 昨收 3951.51）全程未转正 → 按日内最高点 `hi` 三档条件化（`hi>=0` 翻红 / `-0.2<hi<0` 一度逼近平盘 / `hi<=-0.2` 始终未能翻红）。tests 升级**八态**回归——新增 `low_open_recover` 夹具（2026-09-10 真实采集数据脱敏：剔除 6 只真实持仓，7 大指数 + 广度 20.1% 普跌 + 量能 flat 111%，负断言"跌幅收敛并翻红 / 并已收复昨收、由跌转涨"必不出现）。教训：#24 与 #15（"盘中冲高 -0.10%"）同族——**凡断言"翻红/转涨/收复"的方向词必须接实际点位判定**，不能按分支语义想当然；缺陷能逃过回归的根因是夹具形态维度缺失（此前无"回升但最高点仍为负"形态）。
- **v4.3（2026-09-09）**：修复缺陷 #23——模块 04 `build_divergence_note()` 量价提示与量能档脱钩：① `DIVERG_NOTE` 赋值点从 1010 行后移至量能档措辞定义之后（原位置早于 FACTS 导致无法引用 `_VREG`）；② 全部 5 个分支的量能措辞按 `volume_regime` 三态化（缩量/持平/温和放量），expanded 档尾句不再写"量能仍不能放出"；③ 涨分支（cyb>=0.3）补 `up_ratio` 三档二维判定，修复普涨日仍写"指数涨、个股跌"的残留矛盾。tests 升级**七态**回归——新增 `expanded_bear` 放量普跌夹具（2026-09-09 真实采集数据脱敏：仅留 7 大指数、推算全日 117% 放量 + 广度 33% + 创业板 -0.34%，负断言"普跌缩量 / 量能亦未放大 / 量能仍不能放出 / 观望形态"必不出现）。教训：① 凡引用量能事实的文案必须接 `_VREG`，`build_divergence_note` 正是 #21 修复时漏掉的一处；② 函数若引用 FACTS/_VOL_*，赋值点必须在事实层之后（1010→1034 行）；③ 六态夹具全落 flat 档是 #23 逃脱回归的根因——需补真实放量日脱敏夹具。
- **v4.2（2026-09-08）**：修复第 6 形态「指数平 × 个股普涨」批次 2 处硬编码（缺陷 #21-22：#22 模块 04 divergence 平收分支无脑"个股弱"与 62% 普涨矛盾 → 按 up_ratio 二维化补平收×普涨象限"指数平、个股涨、量能平"；#21 模块 04/08 量能措辞无条件"量能偏弱/基本持平至小幅缩量" → 按 volume_regime 三态化并新增 `_VOL_LAB/_VOL_TAIL/_VOL_RISK` 变量）。tests 升级**六态**回归——新增 `index_flat_bull` 平收×普涨夹具（weight_drag 派生，`flip_index_down` 新增 `per_code` 参数逐指数平移：创业板 -0.15% 触发平收分支、其余 ±0.2% 平盘），负断言"个股弱、量能平 / 观望形态"必不出现（教训：第五态 bull_resonance 指数 +0.4% 走 `cyb>=0.3` 分支测不到平收，五态全绿仍复现）。
- **v4.1（2026-09-04）**：修复第 5 形态「指数红 × 个股普涨」批次 5 处硬编码（缺陷 #16-20：模块 08 宽度风险条改二维四象限、模块 08 机会条按上证方向三态、模块 02 资金主线句按 up_ratio 三态、模块 02 探底回升句按 cp 两态、模块 01 开盘档位对称补高开）；tests 升级**五态**回归——新增 `bull_resonance` 普涨共振夹具（由 weight_drag 派生、唯一变量=指数翻红 +0.4%），`run_three_state.py` 支持**负断言**（禁止文案清单），四态全绿不保证第 5 象限（教训：9/4 当天数据全绿仍暴露 #16-20）。
- **v4（2026-09-02）**：修复上表 10 项输出缺陷（编码乱码、走势叙事三分支、板块去重、持仓操作建议与资金事实一致、涨跌幅语病条件化、核心事件与资讯关键词泛化、背离/领涨表述按指数方向条件化）；新增 `--mcp-note` 参数，用于把本期 MCP 实际使用情况（含 hithink 交叉校验与精度说明）写进文末「数据来源」；tests 升级**四态**回归（新增 `weight_drag` 权重拖累夹具，专测「指数方向 × 上涨占比」二维判定），`deploy_to_github.py` 排除 `*_review.html` 测试产物。
- **v3.1（2026-09-01）**：新增「⚠️ 先重试，再降级」节 —— MCP 掉线是间歇性的（12:05 断、13:01 自愈），必须先探测+重试 3 次再降级，并给出降级后回补 + 交叉复测的 patch 脚本范式；westock 补充板块主力资金流用法与「单位万元」。
- **v3（2026-09-01）**：新增「数据源四级降级链」+ hithink-finance MCP 实测要点（`.SH` 走 A股版 / `.TI` 走 index 版 / 不含可转债）；修正东财 `pz` 上限 `100→90` 且仅第 1 页可用，并给出「领跌板块改从 hithink 行业指数本地排序」的解法；新增 Windows/沙箱硬约束（Python DNS 失败走 curl、`/tmp` 不可写用 `.tmpdata/`、代理常被拒、长脚本后台跑）；新增降级披露红线；`holdings.json` 补充可转债代码段支持。
- **v2**：时间一致性三模式（`strict-midday`/`late-snapshot`/`render-archive`）、数据质量闸门、事实层与条件叙事红线、持仓外置到 `holdings.json`。
